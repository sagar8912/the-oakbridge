Act as a Principal Full-Stack Engineer / Solution Architect.

We are continuing the approved Knowledge Base Phase 3 implementation.

IMPORTANT CURRENT STATE:

Step 3 implementation/code work has been completed to the extent possible.

The remaining Step 3D blockers are ENVIRONMENT / LOCAL CONFIGURATION related:

- OOO_API_KEY needs to be configured
- OOO_APP_URL needs to be configured for MCP
- KB-side MCP configuration (KB_API_KEY / KB_ADMIN_URL) needs to be configured
- ooo_pillars_list live smoke validation remains pending until those values are configured
- live E2E regression therefore remains pending
- existing unit/build validation previously passed

I will manually configure the required environment variables/secrets.

DO NOT:
- invent secrets
- modify secrets
- hardcode API keys
- expose secret values
- mark Step 3D live validation as PASS until it is actually tested

Record Step 3D as:

IMPLEMENTATION COMPLETE / LIVE VALIDATION PENDING ENV CONFIGURATION.

Now proceed with STEP 4 ONLY.

Before making ANY code changes:

1. Re-read the existing Phase 3 architecture/audit documentation in this repository.
2. Review all code implemented during Steps 3A, 3B, 3C and 3D.
3. Identify exactly what the approved architecture defines as STEP 4.
4. Do NOT infer Step 4 from generic assumptions.
5. Use the repository documentation and approved architecture as the source of truth.

PHASE 3 ARCHITECTURAL PRINCIPLES MUST REMAIN INTACT:

Vision
→ PEP
→ PDP / centralized authorization
→ KB integration/service boundary
→ approved KB content
→ controlled rendering/deep linking

Preserve:

- deny-by-default authorization
- server-side enforcement
- centralized PDP/PEP direction
- existing RBAC compatibility
- future ABAC/extensible permission architecture
- existing Vision functionality
- KB boundaries
- existing authentication
- existing routes unless explicitly required
- backward compatibility

Do not redesign unrelated functionality.

==================================================
STEP 4A — REPOSITORY & REQUIREMENT ANALYSIS
==================================================

First determine from the repository documentation:

- exact Step 4 objective
- required functionality
- files/components involved
- dependencies on Steps 3A–3D
- acceptance criteria
- security requirements
- tests required

Produce:

STEP 4 REQUIREMENT MATRIX

Requirement
Current implementation
Gap
Required change
File(s)
Risk

IMPORTANT:

Do not modify code during Step 4A.

If Step 4 is not clearly defined in the documentation:

STOP.

Report:

STEP 4 BLOCKED — REQUIREMENTS NOT DEFINED

Do NOT invent requirements.

==================================================
STEP 4B — IMPLEMENT MINIMUM REQUIRED CHANGES
==================================================

Only if Step 4 requirements are clearly supported by repository documentation:

Implement the MINIMUM SAFE changes necessary.

Rules:

1. Reuse existing architecture and utilities.
2. Do not duplicate authorization logic.
3. Do not bypass PEP/PDP.
4. Do not introduce client-side-only security.
5. Do not hardcode user-specific permissions.
6. Do not hardcode secrets.
7. Do not introduce unnecessary DB migrations.
8. Do not change unrelated modules.
9. Preserve existing behavior unless Step 4 explicitly requires otherwise.
10. Keep changes production-oriented and minimal.

For every change report:

FILE:
CHANGE:
WHY REQUIRED:
REQUIREMENT SATISFIED:

==================================================
STEP 4C — SECURITY / AUTHORIZATION VALIDATION
==================================================

Trace any new Step 4 request flow end-to-end.

Explicitly verify:

User
→ UI/action
→ PEP
→ PDP/authorization decision
→ backend/API
→ resource
→ response/rendering

Test at minimum where applicable:

- authorized user
- unauthorized user
- anonymous user
- user without required kb_role
- malformed request
- direct API access bypass attempt
- cross-resource access
- missing configuration
- downstream KB unavailable

Expected principle:

Unauthorized access must fail closed.

No sensitive action should rely solely on UI visibility.

==================================================
STEP 4D — REGRESSION VALIDATION
==================================================

Run all tests that can safely run without the missing Step 3D environment secrets.

Validate:

- TypeScript/type checks
- lint where configured
- unit tests
- relevant integration tests
- build
- existing Vision behavior
- existing KB behavior
- PDP/PEP behavior
- routes/imports
- backward compatibility

Do NOT fake live integration results.

Clearly separate:

PASS
FAIL
BLOCKED BY ENVIRONMENT

The existing Step 3D live tests may remain:

BLOCKED — ENV CONFIGURATION PENDING

That must NOT automatically fail unrelated Step 4 code validation.

==================================================
STEP 4E — FINAL AUDIT
==================================================

After implementation, inspect the git diff.

Check for:

- accidental unrelated changes
- hardcoded secrets
- security regressions
- duplicate authorization logic
- unnecessary dependencies
- unnecessary DB changes
- debug code
- TODOs introduced by this implementation
- broken existing functionality

Do NOT commit or push anything.

==================================================
FINAL OUTPUT
==================================================

Return:

1. STEP 4 OBJECTIVE

2. REQUIREMENTS IMPLEMENTED

3. FILES CHANGED

4. AUTHORIZATION/SECURITY FLOW

5. TEST RESULTS

PASS:
...

FAIL:
...

BLOCKED:
...

6. STEP 3D REMAINING MANUAL CONFIGURATION

Keep this clearly separate from Step 4.

7. REMAINING RISKS / GAPS

8. ONE FINAL VERDICT:

STEP 4 COMPLETE

or

STEP 4 PARTIALLY COMPLETE — BLOCKED

or

STEP 4 BLOCKED — REQUIREMENTS NOT DEFINED

Do not claim COMPLETE unless every code-level Step 4 requirement supported by the repository has been implemented and validated.

STOP after Step 4.

Do NOT proceed to any next phase/step automatically.
Do NOT commit.
Do NOT push.
