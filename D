Act as a Principal Full-Stack Engineer / Solution Architect.

We need to begin the Knowledge Base (KB Admin) work discussed with John in the latest stakeholder meeting.

IMPORTANT:
This first step is ANALYSIS AND PLANNING ONLY.

DO NOT modify code.
DO NOT delete code yet.
DO NOT change the database/schema yet.
DO NOT commit or push.
DO NOT invent requirements.

The objective is to inspect the current repository and create an evidence-based implementation plan aligned with John's requirements.

==================================================
BUSINESS CONTEXT FROM JOHN
==================================================

John's primary concern is that the Knowledge Base application is currently not stable/mature enough for broad user exposure.

He described much of it as scaffolding, with:
- little/no real user adoption yet
- potentially significant backlog
- dead or incomplete functionality
- incomplete permissioning
- potential bugs/breaking-change risk

The TOW → KB integration scaffolding is a good starting point, but we should stabilize and build the KB infrastructure before aggressively exposing/integrating it throughout TOW.

==================================================
1. REVIEW EXISTING KB RESEARCH AND IMPLEMENTATION PLANS
==================================================

John said significant architecture research and implementation planning already exists in the repository.

Search the entire repository, especially:

- docs/
- research/
- plans/
- architecture documentation
- implementation plans
- KB-specific markdown
- TODO/backlog documents
- ADRs/design docs

Identify all existing plans related to:

- Knowledge Base architecture
- schema foundation
- article hierarchy
- permissions
- publishing/approval workflows
- retrieval pipelines
- MCP integrations
- agent integration
- dead-code cleanup
- article generation
- GitHub/CI-CD integration

Do NOT create a competing architecture if an approved/intended architecture already exists.

Create a mapping:

John Requirement
→ Existing Research/Plan
→ Current Implementation
→ Gap
→ Recommended Next Step.

==================================================
2. DEAD CODE / SCAFFOLDING AUDIT
==================================================

John specifically mentioned that the KB contains dead or incomplete functionality.

Audit especially:

- chatbot functionality
- file-upload functionality

Also identify any:

- abandoned routes
- unused components
- unused APIs
- stale feature flags
- placeholder functionality
- obsolete services
- disconnected database code
- misleading UI that appears functional but is not

Classify each finding:

A. Actively used
B. Partially implemented
C. Dead/unreachable
D. Scaffold/future functionality
E. Unknown — requires confirmation

John specifically indicated that old non-working chatbot/file-upload functionality should likely be removed rather than exposed to users and generating bugs.

DO NOT DELETE ANYTHING YET.

Provide exact evidence before recommending deletion.

==================================================
3. KB PERMISSIONING / PDP-PEP ARCHITECTURE
==================================================

John identified permissions as a critical requirement.

Analyze the current KB authorization model.

Determine how permissions currently work for:

- View article
- Create/author article
- Edit article
- Delete article
- Approve article
- Publish article
- Administer KB

Also inspect hierarchy-level access.

John mentioned that a USER or TEAM may potentially be assigned at levels within the Knowledge Base hierarchy.

Analyze whether current architecture supports access inheritance such as:

Parent hierarchy/folder
→ child hierarchy
→ article

Determine:

- current PDP/PEP behavior
- database permission fields
- authorization middleware
- API enforcement
- UI-only checks vs backend enforcement
- role/team/user relationships

Because KB and TOW share the same user base/monorepo, investigate whether existing TOW user identity and permissions can safely serve as the identity foundation for KB.

DO NOT blindly copy TOW permissions.

Document the correct integration architecture first.

==================================================
4. KNOWLEDGE BASE CONTENT MODEL
==================================================

John clarified that KB is NOT only:

"How to use the TOW application."

It should represent the broader organizational Knowledge Base.

Examples include:

- TOW application training
- vendor knowledge
- vendor agreements/expectations
- operational procedures
- IT knowledge
- HR knowledge
- organizational documentation
- policies
- business knowledge

Analyze whether the current schema/content model supports this broader purpose.

Identify limitations without changing schema yet.

==================================================
5. SCHEMA FOUNDATION
==================================================

Review the current KB database/schema against the existing repo architecture plans.

Analyze:

- articles
- article_content
- article_versions
- hierarchy/categories
- slugs
- workflow/status
- approval
- publication
- authorship
- permissions
- teams/users
- audit/history
- metadata/source information

Identify:

- incomplete schema
- unused schema
- duplicate concepts
- architectural inconsistencies
- missing relationships

Do NOT create migrations in this step.

==================================================
6. ARTICLE LIFECYCLE
==================================================

Map the complete intended lifecycle:

Create
→ Draft
→ Edit
→ Review
→ Approve
→ Publish
→ Update/version
→ Archive/delete

Determine what actually works today.

Identify gaps between:

DESIGNED
vs
IMPLEMENTED
vs
WORKING.

John's desired future state is that he should not necessarily need to manually create KB articles through UI.

The architecture should eventually support programmatic article creation with controlled approval/publishing.

==================================================
7. CURSOR / CLAUDE → KB ARTICLE WORKFLOW
==================================================

John described a future workflow similar to:

"I can open Cursor or Claude and say:
create this knowledge article."

Then:

template
→ structured article
→ commit/API/tool workflow
→ KB draft
→ approval
→ publication.

Inspect whether current:

- APIs
- MCP tools
- templates
- workflows
- schemas

already support any portion of this.

Identify the smallest architecture needed to enable this safely.

Do NOT build it yet.

==================================================
8. MCP AND AGENT INTEGRATION
==================================================

John specifically wants the Knowledge Base to eventually become an authoritative backend knowledge source for agents.

Examples discussed:

- IT ticketing agent
- HR-related agent
- LangSmith agents

Instead of relying directly on:

Word documents
PDFs
shared drives

the desired direction is:

Documents
→ structured KB articles
→ KB retrieval/API/MCP
→ LangSmith/agents.

Audit existing:

- KB MCP tools
- retrieval APIs
- search
- hierarchy retrieval
- article retrieval
- authentication
- agent-facing contracts

Identify what is production-ready vs scaffolding.

==================================================
9. DOCUMENT → KNOWLEDGE ARTICLE MIGRATION
==================================================

Analyze how existing:

- Word documents
- PDFs
- shared-drive documents

could eventually be converted into structured KB articles.

Focus on architecture and existing capabilities.

Consider:

source document
→ extraction
→ normalization
→ KB template
→ draft article
→ human approval
→ publication
→ agent retrieval.

Do NOT implement ingestion yet unless an existing approved plan explicitly requires it in the immediate next phase.

==================================================
10. GITHUB / CI-CD → AUTOMATIC TOW KNOWLEDGE ARTICLES
==================================================

This is an important future use case John requested.

Desired conceptual flow:

Feature scoped
→ PRD / markdown documentation
→ code changes
→ PR
→ staging/prod deployment
→ automatically generate/update an END-USER-FACING TOW knowledge article
→ approval
→ publication.

Analyze:

- GitHub Actions
- markdown/PRD structure
- deployment workflow
- KB APIs
- article templates
- approval workflow

Determine what already exists and what is missing.

IMPORTANT:

Do not automatically publish generated AI content directly to production unless the existing approved architecture explicitly supports that.

Prefer:

generate
→ draft
→ review/approval
→ publish.

==================================================
11. TOW ↔ KB INTEGRATION BOUNDARY
==================================================

John said the existing integration scaffolding is a good starting point.

However, broader KB exposure in TOW should not move ahead faster than KB stability/security.

Analyze the current integration.

Separate:

SAFE NOW

from

DEFER UNTIL KB STABLE.

Do not remove working integration without evidence.

==================================================
12. SECURITY AND STABILITY AUDIT
==================================================

Identify risks involving:

- unauthorized article access
- unauthorized creation/edit/delete
- API routes relying only on UI authorization
- hierarchy permission bypass
- direct URL access
- MCP authentication
- service-role exposure
- unsafe secrets
- draft/unapproved article exposure
- cross-user/cross-team access
- destructive operations without authorization

Rank findings:

P0 Critical
P1 High
P2 Medium
P3 Low.

==================================================
REQUIRED FINAL OUTPUT
==================================================

Return ONE structured report:

1. Executive summary

2. Existing KB architecture

3. Existing repo research/plans discovered
   - exact files
   - purpose
   - whether implemented

4. John requirement traceability matrix:

Requirement
→ Existing code/docs
→ Current status
→ Gap
→ Recommended action

5. Dead-code candidates
   - exact files/components
   - evidence
   - safe-to-remove? YES/NO/NEEDS REVIEW

6. Permission/PDP-PEP assessment

7. Schema assessment

8. Article lifecycle assessment

9. MCP/retrieval assessment

10. Agent integration readiness

11. GitHub/CI-CD article-generation readiness

12. Security/stability risks

13. Proposed implementation phases in dependency order.

Suggested dependency order should be evaluated, not blindly assumed:

Phase A — Stabilization/dead-code cleanup
Phase B — Schema/content foundation
Phase C — Permissions/PDP-PEP
Phase D — Article lifecycle/workflows
Phase E — MCP/retrieval foundation
Phase F — Agent/document migration
Phase G — GitHub/CI-CD automatic article generation
Phase H — Broader TOW ↔ KB exposure

For every phase provide:

- scope
- files/modules likely affected
- dependencies
- acceptance criteria
- risks
- tests required

Finish with:

ONE FINAL VERDICT:

READY TO START IMPLEMENTATION
or
BLOCKED — REQUIREMENTS/ARCHITECTURE DECISION NEEDED

If READY, identify exactly ONE recommended first implementation phase.

STOP.

Do not modify code.
Do not commit.
Do not push.
Wait for approval before implementation.
