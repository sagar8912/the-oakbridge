Act as a Principal Full-Stack Engineer / Solution Architect.

We are continuing the existing Phase 3 implementation.

Current verified state from your previous execution:

STEP 3 = PARTIALLY COMPLETE

You reported:
- Step 3A implementation is in place
- Step 3B implementation is in place
- Step 3C implementation is in place
- Step 3D and full regression validation remain blocked by ExecutionPolicy and/or unset environment secrets/configuration
- The blocker is environmental/configuration-related, not a missing implementation design
- Step 4 has NOT started

Your task now is to CLOSE OUT STEP 3 SAFELY.

IMPORTANT:
Do NOT start Step 4.
Do NOT expand scope.
Do NOT redesign the approved architecture.
Do NOT introduce unrelated refactoring.
Do NOT invent secret values.
Do NOT expose secrets.
Do NOT make destructive database changes.

STEP 1 — RE-ANALYZE CURRENT STATE

First inspect all code changes currently made for Phase 3.

Verify exactly what was implemented in:
- 3A
- 3B
- 3C

For every implementation, trace the complete code path:
UI/client
→ request
→ PEP/enforcement
→ PDP/policy decision
→ permission/attribute source
→ database/data access
→ response/error handling.

Confirm that implementation still follows the approved Phase 3 architecture.

Identify:
- incomplete code
- TODOs
- placeholders
- hard-coded temporary logic
- fail-open authorization behavior
- duplicated authorization logic
- missing error handling
- missing auditability
- type inconsistencies

Do not modify anything during this analysis.

STEP 2 — IDENTIFY EXACT 3D BLOCKERS

Determine exactly what is preventing Step 3D from executing.

For each blocker report:

- blocker name
- exact command/test being blocked
- whether caused by PowerShell ExecutionPolicy, missing environment variable, missing secret, unavailable service, or another reason
- whether it can be resolved safely without changing application architecture

For environment variables/secrets:

Only report the VARIABLE NAMES required.

Never print secret values.

Clearly separate:

REQUIRED FOR LOCAL TESTING
REQUIRED FOR STAGING
REQUIRED FOR PRODUCTION

If there is a safe way to run tests without changing machine-wide ExecutionPolicy, provide the exact command.

Prefer temporary/process-scoped solutions over permanent machine configuration changes.

STEP 3 — EXECUTE ALL SAFE VALIDATION POSSIBLE

Run every validation that can safely execute in the current environment.

Where applicable run:

- targeted unit tests for Phase 3
- PDP tests
- PEP tests
- authorization/permission tests
- TypeScript type checking
- lint
- relevant integration tests
- regression tests around existing role-based behavior

Do not claim PASS for anything that was not actually executed.

Report each validation as:

PASS
FAIL
BLOCKED
NOT APPLICABLE

For failures provide:
- exact failing test
- root cause
- whether failure was introduced by Phase 3

Only fix a failure if it is clearly inside the already-approved Step 3 scope.

Do not make speculative fixes.

STEP 4 — SECURITY / PERMISSION VALIDATION

Specifically verify the architecture John requested:

Roles remain the BASE permission set.

Example:
Super Admin may receive a collection of default capabilities through its role.

But individual users must be capable, through the Phase 3 foundation, of receiving additional granular permissions/attributes independently of their role.

Validate that the architecture supports the future model:

ROLE BASE PERMISSIONS
+
INDIVIDUAL USER ATTRIBUTES/PERMISSIONS
=
EFFECTIVE PERMISSIONS

Example future use case:

A user who is NOT Super Admin may eventually receive a specific permission such as:

goal.delete

without being promoted to Super Admin.

Verify that PDP is the centralized decision point and PEP correctly enforces the resulting decision.

Check that sensitive authorization is not based only on hidden UI buttons.

Backend/server-side enforcement must exist.

Check fail-safe behavior:

If PDP/permission resolution fails, sensitive operations must not silently become allowed.

STEP 5 — BACKWARD COMPATIBILITY

Verify existing users who only have role-based permissions continue to behave exactly as expected.

Phase 3 foundation must not unintentionally break:

- Super Admin
- Admin
- Team Admin
- Team Owner
- normal users

Do not change existing business permissions unless explicitly required by the approved architecture.

STEP 6 — STEP 3D CLOSEOUT

Attempt Step 3D only when prerequisites available in the current environment allow it.

If configuration is missing, do NOT fake a successful result.

Instead give me exact manual setup instructions.

After configuration is available, validate the full request path and regression behavior.

STEP 7 — FINAL REPORT

At the end provide:

1. Files changed for Phase 3
2. What 3A implemented
3. What 3B implemented
4. What 3C implemented
5. Status of 3D
6. Tests actually executed
7. PASS/FAIL/BLOCKED table
8. Remaining environment/config blockers
9. Security findings
10. Backward compatibility findings
11. Any technical debt created
12. Exact manual actions I need to perform
13. Production-readiness risks

Then provide ONE final verdict:

STEP 3 COMPLETE — READY FOR STEP 4

or

STEP 3 PARTIALLY COMPLETE — BLOCKED

or

STEP 3 FAILED — FIX REQUIRED

If blocked, explicitly tell me the minimum actions needed to unblock it.

CRITICAL STOP CONDITION:

DO NOT PROCEED TO STEP 4.

Even if Step 3 becomes fully green, STOP after the Step 3 closeout report and wait for my explicit approval before starting Step 4.
