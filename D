I need to implement the following Phase 3 requirement for the existing TOW App / KB-Admin App:

PHASE 3: MCP Wrappers / Context-Callable APIs
Goal: Create an MCP Wrapper for Agentic Development.

Requirements:
1. Harden APIs and security so TOW + Knowledge Base capabilities can be safely wrapped and exposed as callable services/tools (MCP-style) for future agentic orchestration.
2. Ensure the architecture remains model-agnostic and aligned with Azure guardrails. Avoid vendor/model lock-in and avoid introducing a premature "AI-in-a-box" architecture.
3. Use a centralized MCP tool-server approach for integrating TOW and Knowledge Base capabilities, with LangSmith integration/tracing where applicable.

IMPORTANT:
DO NOT modify, create, delete, rename, or refactor any files in this step.
This is ANALYSIS ONLY.

First, deeply analyze the complete repository and existing architecture before recommending anything.

Analyze:

1. TOW App architecture
   - Frontend
   - Backend
   - API routes/endpoints
   - Service/business logic
   - Database/data-access layer
   - Authentication
   - Authorization
   - Existing PDP/PEP/RACI implementation
   - Logging/auditing
   - Error handling
   - Environment/configuration structure

2. KB-Admin / Knowledge Base architecture
   - Existing APIs
   - Search/retrieval functionality
   - Document/content operations
   - Authentication/authorization
   - How TOW currently communicates with KB-Admin, if applicable

3. Existing AI/agent/LangSmith-related implementation
   - Search for LangSmith, LangChain, agents, tools, MCP, LLM providers, Azure AI/OpenAI, or similar integrations.
   - Determine what already exists and what can safely be reused.

4. Existing Azure-related architecture/configuration.

5. Identify all existing APIs/capabilities that could potentially become MCP tools.

For each candidate capability, classify it as:

- Read-only / low risk
- Write / medium risk
- Delete/admin/security-sensitive / high risk

Do NOT assume MCP is required everywhere.

Then provide:

A. CURRENT ARCHITECTURE
Explain the current relevant architecture and data/request flow.

B. EXISTING REUSABLE COMPONENTS
Show what already exists that Phase 3 can reuse.

C. GAP ANALYSIS
Map each Phase 3 requirement to:
- Already implemented
- Partially implemented
- Missing

D. MCP CANDIDATE CAPABILITIES
List possible MCP tools, but DO NOT implement them.

E. SECURITY ANALYSIS
Explain how existing authentication + PDP/PEP/RACI authorization works and where MCP must integrate without bypassing security.

F. RECOMMENDED ARCHITECTURE
Propose the minimum-change architecture for:
AI/Agent
→ Central MCP Tool Server
→ Existing TOW/KB APIs or service layer
→ PEP/PDP authorization
→ Business logic
→ Data

Do not duplicate existing business logic inside MCP wrappers.

G. LANGSMITH CLARIFICATION
Analyze the requirement "centralized MCP tool server within LangSmith."

Clearly distinguish:
- MCP server/tool hosting
- Agent orchestration
- LangSmith tracing/observability/evaluation

Do not incorrectly assume LangSmith itself must host the MCP server unless the existing architecture/documentation proves that.

H. IMPLEMENTATION PLAN
Give a safe step-by-step implementation plan with exact files likely to be:
- created
- modified
- reused

I. RISKS / OPEN QUESTIONS
Identify anything that must be clarified before implementation.

Do not write any implementation code yet.

Primary principles:
- Minimum necessary changes
- No breaking existing functionality
- No duplicated business logic
- No bypassing PDP/PEP/RACI
- Model-agnostic
- Azure-compatible
- Secure by default
- Backward compatible
