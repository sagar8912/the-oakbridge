We have confirmed:

PHASE 3 CODE COMPLETE — STEP 3D LIVE VALIDATION PENDING.

Now help me complete the manual validation checklist ONE STEP AT A TIME.

Start with STEP 1 ONLY:

Configure OOO_API_KEY correctly for:
1. the OOO application local environment
2. the MCP configuration

Requirements:

- First inspect the repository/configuration and confirm the exact OOO app path and exact environment file that should be used.
- Confirm the exact MCP configuration file/location expected by this project.
- Do NOT invent or generate a secret for me.
- Do NOT print any existing secret values.
- Do NOT hardcode secrets into source code.
- Do NOT commit any secrets.
- Do NOT modify application/business logic.
- Use placeholders such as <YOUR_LOCAL_OOO_API_KEY> when showing examples.

Explain exactly:

A. Which file I need to open/create for the OOO app.
B. The exact variable line/format I need to add using a placeholder.
C. Which MCP configuration file I need to open/create.
D. Exactly where OOO_API_KEY must be configured there using the same placeholder.
E. Whether the two values must be identical.
F. How I can safely generate/use a local development secret if no existing local secret is available.
G. How to verify that both configurations are present WITHOUT displaying the secret.

IMPORTANT:

Do not proceed to OOO_APP_URL.
Do not start applications.
Do not run smoke tests.
Do not make unrelated changes.

STOP after giving me the exact Step 1 instructions and wait for me to complete them.
