Proceed with STEP 3: IMPLEMENT THE MINIMUM SAFE PHASE 3 FOUNDATION based strictly on the Step 1 repository analysis and Step 2 approved target architecture.

This is now an IMPLEMENTATION step, but scope must remain tightly controlled.

DO NOT implement all of Phase 3.

The approved Step 3 scope from the architecture analysis is:

3A. Harden OOO dual-auth on the identified 8 MVP routes
3B. Fix/document environment and API-key configuration required for MCP
3C. Fix existing KB MCP tool contract drift, starting with kb_hierarchy and then kb_search
3D. Validate one existing OOO read tool end-to-end and run automated + manual smoke validation

IMPORTANT ARCHITECTURAL CONSTRAINTS:

- REUSE the existing MCP packages and patterns.
- REUSE mcp-shared.
- REUSE validateApiKey and existing security mechanisms.
- REUSE existing APIs and business logic.
- REUSE existing PEP/PDP/RLS authorization.
- DO NOT create a new mega-gateway.
- DO NOT create a new MCP server unless absolutely required by existing architecture.
- DO NOT add new MCP tools in this step.
- DO NOT implement write-capable agent tools.
- DO NOT implement LangSmith hosting.
- DO NOT migrate models/providers.
- DO NOT perform Azure deployment.
- DO NOT refactor unrelated code.
- DO NOT weaken any existing security to make tests pass.

Before editing each area, inspect the exact implementation and confirm the minimum required change.

==================================================
STEP 3A — HARDEN OOO DUAL-AUTH
==================================================

The previous analysis identified missing/incomplete dual-auth on 8 OOO MVP routes.

First, identify and list the exact 8 routes and their file paths.

For each route document BEFORE changing:

- current authentication
- current authorization
- whether validateApiKey is present
- whether user/session authentication is present
- whether PEP/PDP/RLS is involved
- current security gap

Then implement the existing repository's intended DUAL-AUTH pattern.

IMPORTANT:

Do NOT invent a new authentication system.

Follow the repository's existing approved dual-auth pattern.

The intended principle is that routes used through MCP must not become less secure than normal application access.

Where supported by the existing architecture, distinguish:

1. Service/API authentication
   Validates the trusted calling service using the existing API-key/service mechanism.

2. User/application authorization
   Preserves existing user/session/context and PEP/PDP/RLS authorization where required.

Do not trust MCP/tool input parameters to self-declare:

- user identity
- role
- team
- tenant
- permissions
- authorization level

Do not bypass existing RLS/PDP/PEP.

Do not globally convert all application routes to service-role access.

Use the minimum change required on the identified 8 routes.

SECURITY TESTS REQUIRED:

For each affected route or representative route groups, test:

- valid normal application/user authentication still works
- valid intended MCP/service authentication works only as designed
- missing authentication fails
- invalid API key fails
- unauthorized user/context fails where applicable
- authorization/RLS is not bypassed
- existing frontend behavior is not broken

Fail closed.

==================================================
STEP 3B — ENVIRONMENT / API KEY HARDENING
==================================================

The analysis found unset/mismatched environment keys.

Audit the exact environment variables used by:

- mcp-ooo
- mcp-kb-admin
- mcp-shared
- apps/ooo
- apps/kb-admin

Identify:

- variable name
- where it is read
- where it is documented
- whether it is required/optional
- naming mismatches
- unsafe fallback behavior
- missing validation

DO NOT print or expose actual secret values.

DO NOT hardcode secrets.

Fix only configuration CONTRACT problems in code/docs/examples.

Examples:

- inconsistent variable names
- missing startup validation
- misleading .env.example entries
- unsafe fallback behavior
- mismatched API-key header expectations

If actual secret values must be manually configured by a developer or deployment environment:

DO NOT invent values.

Clearly tell me:

MANUAL CONFIGURATION REQUIRED

and provide:

VARIABLE NAME
WHERE IT MUST BE SET
WHAT COMPONENT USES IT
HOW TO VERIFY IT WITHOUT REVEALING THE SECRET

Ensure production-sensitive keys fail safely when required.

Do not break local development unnecessarily.

==================================================
STEP 3C — FIX EXISTING KB TOOL CONTRACT DRIFT
==================================================

Do NOT create new KB tools.

Audit the existing:

1. kb_hierarchy
2. kb_search

For each trace:

MCP tool schema
→ MCP implementation
→ HTTP/API request
→ backend route
→ request schema
→ response schema
→ MCP response

Identify the exact contract drift.

Examples to check:

- wrong endpoint
- stale parameter names
- missing required parameters
- response-shape mismatch
- outdated tool description
- authentication mismatch
- inconsistent error handling

Fix kb_hierarchy first.

Run tests.

Only after kb_hierarchy is validated, fix kb_search.

Maintain backward compatibility where reasonably possible.

Do not duplicate search/hierarchy business logic inside MCP.

The MCP tool must remain a thin adapter over the existing KB application capability.

==================================================
STEP 3D — END-TO-END MCP FOUNDATION VALIDATION
==================================================

After 3A, 3B and 3C are complete, validate ONE EXISTING LOW-RISK READ-ONLY OOO MCP TOOL end-to-end.

Do not create a new tool.

Choose the safest existing read-only tool based on repository evidence.

Show the exact execution path:

MCP Client / stdio
        ↓
Existing mcp-ooo tool
        ↓
Shared MCP/API client
        ↓
x-api-key / intended service authentication
        ↓
OOO API
        ↓
Existing authentication/authorization
        ↓
PEP/PDP/RLS where applicable
        ↓
Existing business/data layer
        ↓
Response back through MCP

Validate:

1. MCP server starts successfully.

2. Existing tool discovery works.

3. Selected read-only tool can be invoked successfully with valid configuration/authentication.

4. Missing/invalid credentials fail safely.

5. Unauthorized data cannot be retrieved.

6. Existing application functionality still works.

7. No direct database access was introduced into MCP.

8. No business logic was duplicated.

9. No new tools were created.

10. No secrets appear in logs/errors/output.

Also validate:

kb_hierarchy
and
kb_search

through their existing MCP paths after contract fixes.

==================================================
REGRESSION TESTING
==================================================

Run all relevant existing:

- unit tests
- integration tests
- MCP tests
- authentication tests
- authorization tests
- type checks
- lint
- build checks

Do not hide pre-existing failures.

Clearly distinguish:

NEW FAILURE CAUSED BY STEP 3

vs

PRE-EXISTING FAILURE

Any security-sensitive failure introduced by Step 3 must be fixed before completion.

==================================================
CHANGE CONTROL
==================================================

Before making changes:

Record the current git status.

Do not modify unrelated existing user changes.

After implementation provide the exact changed-file list.

For EVERY changed file explain:

- what changed
- why
- which Step 3 requirement required it

Do not perform broad formatting/refactoring.

Do not delete working code unless clearly necessary.

==================================================
STOP CONDITIONS
==================================================

STOP and ask for clarification instead of guessing if:

1. Implementing dual-auth requires choosing between materially different security models not established by repository evidence.

2. Safe end-user impersonation/delegation is required.

3. Actual production/staging secret values are required.

4. A change would weaken existing PDP/PEP/RLS.

5. A change would require creating a new centralized gateway.

6. A change would require Azure infrastructure/deployment.

7. A change would require introducing new write capabilities.

For configuration values, code as far as safely possible and mark manual configuration instead of inventing secrets.

==================================================
FINAL REPORT
==================================================

Return:

1. STEP 3A — DUAL-AUTH

Routes identified:
[table]

Changes:
[details]

Tests:
[PASS/FAIL]

2. STEP 3B — ENVIRONMENT/API KEYS

Problems found:
[table]

Fixed:
[details]

Manual configuration required:
[details without secrets]

3. STEP 3C — KB TOOL DRIFT

kb_hierarchy:
- root cause
- fix
- tests

kb_search:
- root cause
- fix
- tests

4. STEP 3D — E2E VALIDATION

Selected existing OOO read tool:
[name]

Execution path:
[diagram]

Results:
[PASS/FAIL]

5. FILES CHANGED

Exact list with reason.

6. REGRESSION RESULTS

Tests/build/lint/type-check.

7. SECURITY VALIDATION

Confirm PASS/FAIL:

- API/service auth enforced
- existing user authorization preserved
- PDP/PEP/RLS not bypassed
- invalid credentials rejected
- unauthorized access rejected
- no secrets exposed
- no hardcoded secrets
- no direct DB access added to MCP
- no duplicate business logic
- no new MCP tools
- no mega-gateway
- no LangSmith hosting changes

8. REQUIREMENT PROGRESS

Phase 3 Requirement 1:
Current status and evidence.

Phase 3 Requirement 2:
Current status and evidence.

Phase 3 Requirement 3:
Current status and evidence.

Do NOT declare all of Phase 3 complete yet.

9. REMAINING BLOCKERS / OPEN DECISIONS

Clearly separate:

BLOCKS NEXT STEP

DOES NOT BLOCK READ-ONLY MCP FOUNDATION

10. FINAL VERDICT

STEP 3:
COMPLETE / PARTIALLY COMPLETE / BLOCKED

Explain based only on code and test evidence.

Do not automatically proceed to Step 4.
STOP after the Step 3 report.
