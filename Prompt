We need to implement a new enhancement for Goal Level configuration based on the client's requirements.

IMPORTANT:
Do NOT immediately start coding.
First analyze the current implementation and explain the existing architecture before making any changes.

Background:
Currently, the application uses a hardcoded mapping between Team Level and Goal Level. When a user creates a Goal, the available Goal Levels are automatically determined by the selected Primary Team's Team Level.

This behavior was originally intentional, but it is now too rigid for business needs.

Client Requirement:
Instead of hardcoding Goal Levels based on Team Level, introduce a configurable Team Setting called "Allowed Goal Levels".

Requirements:

1. Preserve existing behavior as the default.
   - If no custom configuration exists for a team, continue using the current Team Level → Goal Level mapping.
   - This ensures backward compatibility.

2. Add a new Team Setting:
   - Name: Allowed Goal Levels
   - Type: Multi-select
   - Visible only to Super Admin/Admin users.
   - Place it in Team Settings alongside Transfer Targets since it follows the same configuration pattern.

3. Goal Creation Flow:
   - When the user selects a Primary Team during Goal creation:
       a. Check whether the team has a custom Allowed Goal Levels configuration.
       b. If configured, display only those Goal Levels.
       c. Otherwise, use the existing hardcoded/default Team Level mapping.

4. Backend:
   - Store Allowed Goal Levels for each Team.
   - Keep the current mapping as the fallback/default.
   - Validate Goal creation server-side to prevent unauthorized Goal Levels.

5. Frontend:
   - Build the Team Settings UI for Allowed Goal Levels.
   - Update the Goal Creation dialog to consume the new configuration.

6. Security:
   - Only authorized admins can modify Allowed Goal Levels.
   - Normal users should only see the filtered Goal Levels during Goal creation.

7. Backward Compatibility:
   - Existing teams and existing goals must continue working without any migration issues.

Before implementing:
1. Find where the current Team Level → Goal Level mapping exists.
2. Identify all backend and frontend files involved.
3. Explain the current flow.
4. Explain the implementation approach.
5. Identify any migration or database schema changes required.
6. Only after the analysis, implement the solution.

Finally provide:
- List of modified files
- Summary of backend changes
- Summary of frontend changes
- Database/schema changes
- API changes
- Validation logic
- Test cases to verify the implementation
- Potential risks and edge cases
