Proceed with TOW CHANGE 1:

PER-USER ADDITIONAL PERMISSIONS UI.

John explicitly requested that the current additional-permissions
multi-select/search picklist be replaced with a visible True/False Boolean
checkbox list on the User Edit/Profile screen.

Focus ONLY on TOW.

Do NOT touch KB Admin or introduce KB permissions.

==================================================
REQUIREMENT
==================================================

On the existing Edit User / User Profile administration screen:

Replace the current additional-permission multi-select UI with a clear,
readable checkbox-based permissions section.

Instead of requiring an administrator to:

search
→ scroll
→ select permission names

the administrator should be able to immediately see the available additional
permissions and check/uncheck them.

Conceptually:

Additional Permissions

☐ Permission A
☑ Permission B
☐ Permission C
☑ Permission D

Use the actual permission names/labels already defined by the application.

Do NOT invent duplicate permissions.

==================================================
IMPORTANT PERMISSION MODEL
==================================================

Preserve the existing architecture:

ROLE/SYSTEM PERMISSIONS
=
baseline capabilities inherited from the user's role

ADDITIONAL USER PERMISSIONS
=
extra capabilities explicitly assigned to that individual

EFFECTIVE PERMISSIONS
=
role/system baseline + additional individual permissions

Do NOT accidentally remove existing role permissions when an additional
permission checkbox is unchecked.

Do NOT redesign the permission model unless required to fix an actual bug.

==================================================
UI REQUIREMENTS
==================================================

1. Display available additional permissions as checkboxes.

2. Existing assigned additional permissions must load as checked.

3. Unassigned additional permissions must load as unchecked.

4. Administrator can check/uncheck permissions.

5. Save must persist the correct values.

6. Reopening Edit User must display the persisted state correctly.

7. Use readable labels/descriptions if they already exist in permission
metadata.

8. If the permission list is large, organize it cleanly using existing
categories/groups where available.

Do not create arbitrary categories.

==================================================
SECURITY
==================================================

UI checkboxes are only permission-management UI.

Actual authorization must continue to be enforced through the existing
backend/PDP/PEP architecture.

Verify:

User
→ action
→ PEP
→ PDP/effective permission
→ allow/deny

Do not rely only on hiding/showing buttons.

==================================================
BACKWARD COMPATIBILITY
==================================================

Existing users must retain their current:

- roles
- system/default permissions
- individually assigned additional permissions

Do not reset existing permission data.

Avoid database migrations unless absolutely necessary.

==================================================
TESTS
==================================================

Test:

1. User with no additional permissions.
2. User with one additional permission.
3. User with multiple additional permissions.
4. Add permission → Save → Reload.
5. Remove additional permission → Save → Reload.
6. Verify role-based baseline remains unchanged.
7. Verify authorized operation succeeds.
8. Verify unauthorized operation is denied server-side.

Run relevant:
- unit tests
- type checks
- build
- permission/PDP/PEP tests

==================================================
FINAL REPORT
==================================================

Return:

FILES CHANGED
WHAT CHANGED
PERMISSION DATA FLOW
PDP/PEP IMPACT
TEST RESULTS
MANUAL UI TEST STEPS

Do NOT touch KB Admin.

Do NOT commit or push.

STOP after completing this TOW change.
