Correction: We do NOT run a local Supabase/Postgres instance.

This repository connects directly to the STAGING Supabase environment.

Do NOT start Docker, local Supabase, or a local PostgreSQL instance.
Do NOT change the application configuration to introduce local Supabase.

For Wave 2 verification:

1. Inspect the repository's existing staging Supabase configuration/environment and use the same connection mechanism already used by the project.

2. Do NOT expose, print, or modify any Supabase credentials, service-role keys, passwords, or secrets.

3. Run the Wave 2 tests against the configured staging environment using the repository's existing test/database mechanism.

4. If the current tests are hard-coded to 127.0.0.1:54322, STOP and identify exactly where that assumption comes from. Do not rewrite application code just to bypass it.

5. Verify whether the repository has an existing staging/test command, environment variable, or Supabase connection setup that should be used instead.

6. Run:
   - all Wave 2 tests
   - all Wave 1 tests
   - relevant KB permission tests

7. Do NOT start Wave 3 or make any Wave 3+ changes.

8. Do NOT commit anything.

At the end report:
- exact test commands executed
- whether they connected to STAGING Supabase
- pass/fail result
- any tests that could not safely run against staging
- files changed
- confirmation that kb_user_feature_permissions was NOT dropped
- confirmation that no Wave 3+ work was started

IMPORTANT:
Because this is STAGING, do not perform destructive operations or modify production-like data merely to make tests pass. If a test requires destructive/reset operations, stop and report it instead.
