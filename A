I want to update the existing permission-management document that you created earlier.

Before making any changes, first review the existing document/content and identify:
1. What content is already present.
2. What needs to be changed based on the updated KB role model.
3. What content should remain unchanged.
4. What information is missing for creating a client-ready PowerPoint.

IMPORTANT ROLE CORRECTION:

The presentation must NOT use "KB Super Admin" or "Super Admin".

The correct KB roles are:

1. KB Admin
2. KB Editor
3. KB Reader / Viewer

Remove or replace incorrect terminology such as:
- KB Super Admin
- Super Admin
- KB User (where it is being used as a role)

The final permission model should focus only on:
KB Admin → KB Editor → KB Reader / Viewer

The presentation should explain:
- KB Admin
- KB Editor
- KB Reader / Viewer
- Feature-level permissions
- Content-wise permissions
- How these work together

TARGET PPT STRUCTURE:

Slide 1 — KB Roles & Permissions
Slide 2 — KB Role & Access Model
Slide 3 — Permission Management
Slide 4 — Permission Management Flow
Slide 5 — Content-wise Permissions
Slide 6 — Content Permission Flow
Slide 7 — End-to-End Permission Workflow

Now, in addition to updating the document content, I want you to add the required coding part so that this document/content can be used to generate a NEW professional PowerPoint.

IMPORTANT:
Do not just give me a textual explanation of how to create the PPT.

I want actual working code that can generate the PPT from the approved content.

The code should:

1. Create a PowerPoint file (.pptx).
2. Generate all 7 slides.
3. Use professional client-ready formatting.
4. Use consistent fonts, spacing, alignment and typography.
5. Create clean role/permission diagrams using PowerPoint shapes.
6. Use arrows/connectors between workflow steps.
7. Use separate visual sections for:
   - KB Roles
   - Feature Permissions
   - Content Permissions
8. Keep diagrams simple and readable.
9. Avoid overcrowding slides.
10. Make the PPT editable — use PowerPoint text boxes, shapes and connectors instead of screenshots wherever possible.
11. Keep the design suitable for a professional client presentation.
12. Make sure no content is clipped or overflowing outside slide boundaries.
13. Use consistent terminology throughout the presentation.

CONTENT MODEL:

KB Admin:
- Administrative control of the Knowledge Base.
- User/role management.
- Permission management.
- Feature-level permission management.
- Content-wise permission management.

KB Editor:
- Can edit assigned/permitted KB content.
- Access depends on configured feature and content permissions.

KB Reader / Viewer:
- Read/view-only access to permitted KB content.
- No editing capability unless explicitly supported by the configured permissions.

IMPORTANT:
Do not invent functionality that is not present in the existing application.
If something is uncertain, mark it as:
"To be validated with product/team"

PERMISSION MODEL:

Role
   ↓
Feature Permissions
   ↓
Content-wise Permissions
   ↓
Final Access

END-TO-END FLOW:

KB Admin
   ↓
User / Role Assignment
   ↓
KB Editor OR KB Reader / Viewer
   ↓
Permission Management
   ↓
Feature Permissions
   ↓
Content-wise Permissions
   ↓
Final User Access

Also create separate diagrams for:

A. KB Role Model

KB Admin
KB Editor
KB Reader / Viewer

B. Permission Management

KB Admin
   ↓
Permission Management
   ↓
Select User / Role
   ↓
Manage Feature Permissions
   ↓
Apply Permissions

C. Content Permission

KB Admin
   ↓
Content Management
   ↓
Select User / Role
   ↓
Configure Content Access
   ↓
Apply Access

The document should contain both:
1. The final approved presentation content.
2. The complete PPT-generation code.

Prefer Python with python-pptx if that is appropriate for the existing project/environment.

Before writing the code, inspect the existing project/files and determine:
- Which file currently contains the permission content.
- Which file is the current HTML/document source.
- Whether there is already PPT-generation code.
- What libraries/dependencies are already available.

Do not unnecessarily recreate existing functionality.

If an existing PPT-generation script exists, modify/improve it instead of creating a duplicate.

Create the code in a clearly separated section/file so I can run it independently to generate the new PPT.

Expected output:

1. Updated permission-management content/document.
2. PPT-generation script.
3. Clear instructions for running the script.
4. Generated PPT file, if the environment supports generating it.
5. A short list of assumptions or items that still need validation.

Most importantly, review the existing content first and preserve anything that is already correct. Only make the changes required for the updated KB Admin / KB Editor / KB Reader-Viewer model.
