I want to implement an MCP (Model Context Protocol) Wrapper for our Knowledge Base application.

Current Stack:
- FastAPI backend
- React frontend
- PostgreSQL database
- LangSmith for observability
- Anthropic Claude model

Goal:
Expose the Knowledge Base as MCP tools so any AI agent can automatically invoke them.

Requirements:

1. Create a new folder

src/
   mcp/
       server.py
       tools.py
       schemas.py
       config.py

2. Use the official MCP Python SDK.

3. Create an MCP server exposing these tools:

- search_knowledge_base(query)
- get_article(article_id)
- list_categories()

4. Each tool should internally call the existing Knowledge Base service instead of duplicating business logic.

5. Return structured JSON responses.

6. Include metadata:
- tool name
- description
- input schema
- output schema

7. Make the implementation modular and production ready.

Do NOT change any existing APIs.

Only build the MCP wrapper around existing services.

Generate clean code with proper typing and comments.

