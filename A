
The password recovery endpoint (/recover) is returning HTTP 504 with "Context deadline exceeded".

Please debug the entire forgot password email flow.

Check:
1. Whether the SMTP connection is actually established.
2. Whether the mail transporter.verify() succeeds.
3. Whether smtp.office365.com:587 is reachable.
4. Whether STARTTLS is being used correctly.
5. Whether the SMTP username and password are loaded from environment variables.
6. Add detailed logs before and after every step:
   - create transporter
   - verify transporter
   - sendMail()
   - SMTP response
   - full stack trace on failure
7. Increase timeout values for SMTP connection and send operation.
8. If sendMail hangs, identify why and fix it.
9. Ensure the API returns the real SMTP error instead of only "Context deadline exceeded".
10. Do not change business logic. Only diagnose and fix the email delivery path.
