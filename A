Proceed with PHASE B ONLY — Knowledge Base Schema/Content Foundation.

Phase A (Knowledge Base Stabilization and confirmed dead-code cleanup) is complete.

IMPORTANT:
- Do NOT start Phase C or the full hierarchical PDP implementation.
- Do NOT make assumptions about USER/TEAM inheritance or permission behavior.
- Preserve all existing working KB Admin and TOW functionality.
- Do not introduce a new architecture unless it is strictly required by the approved Phase B scope.
- Do not commit or push changes. Stop after implementation, validation, and reporting.

Before making changes:
1. Review the existing Phase A work and all relevant analysis/planning documents, especially:
   - kb-permission-model-analysis.md
   - existing KB stabilization/stakeholder plan
   - Track 1 documentation
   - T1-05 and T1-06 findings
2. Inspect the current database/schema/migrations and actual application usage.
3. Confirm the exact Phase B scope from the repository documentation.
4. Reuse the existing architecture and patterns.

Implement Phase B, beginning with the documented Track 1 leftovers:

T1-05 — documents.ui_section

1. Inspect the `documents` table/schema and all code references to `ui_section`.
2. Determine its current intended purpose and whether it is actively used.
3. Check for:
   - missing/incorrect schema definition
   - inconsistent values
   - null/invalid values
   - frontend/backend mismatches
   - migration issues
   - legacy/dead usage
4. Implement ONLY the fix supported by the existing requirements and repository evidence.
5. Do not invent new section/category behavior.
6. Preserve backward compatibility with existing KB documents.

T1-06 — orphan file_upload row cleanup

1. Inspect the existing `file_upload` data model, relationships, foreign keys, and usage before changing anything.
2. Identify exactly how orphan `file_upload` rows can occur.
3. Implement a safe cleanup approach for confirmed orphan records only.
4. Ensure valid file/document relationships cannot accidentally be deleted.
5. Make the operation safe and repeatable/idempotent where appropriate.
6. If a migration or cleanup script is required:
   - follow existing repository conventions
   - make it auditable
   - avoid destructive assumptions
   - document rollback/recovery considerations
7. Prevent future orphan creation only if the correct fix is clearly supported by the current architecture and requirements.

Then complete the remaining Phase B Schema/Content Foundation work ONLY if it is explicitly documented as approved/in-scope in the existing plan.

For every additional Phase B item:
- Verify the requirement from repository documentation first.
- Do not infer or implement unconfirmed product behavior.
- Keep changes minimal and backward-compatible.

STRICT BOUNDARY:

Do NOT implement Phase C / full hierarchical PDP.

Specifically, do not implement or guess:
- USER vs TEAM inheritance rules
- new permission inheritance behavior
- hierarchical PDP behavior dependent on those rules
- speculative role/access logic

If any Phase B task depends on unresolved USER/TEAM inheritance described in `kb-permission-model-analysis.md`, mark it as BLOCKED and leave it unchanged.

VALIDATION:

After implementation:

1. Run relevant:
   - lint
   - type checks
   - build
   - existing automated tests

2. Check for:
   - broken imports
   - TypeScript errors
   - migration/schema errors
   - broken API contracts
   - runtime errors

3. Verify existing behavior remains intact:
   - KB Admin navigation
   - Knowledge Base pages
   - document/content loading
   - file upload/document relationships
   - direct/deep links
   - TOW deep links
   - refresh behavior

4. Specifically validate:
   - documents.ui_section behavior/data integrity
   - orphan file_upload cleanup safety
   - valid file records remain untouched
   - existing documents remain accessible

At the end, provide a detailed Phase B report with:

A. Phase B scope discovered from repository documentation
B. Files changed and why
C. Database/schema/migration changes
D. T1-05 implementation details
E. T1-06 implementation details
F. Any other approved Phase B work completed
G. Tests/build/type-check results
H. Manual smoke tests I need to perform
I. Anything intentionally NOT changed
J. Blocked items requiring John/product clarification
K. Confirmation that Phase C and unresolved USER/TEAM inheritance were NOT implemented

Do not commit.
Do not push.
STOP after Phase B and wait for my approval before proceeding further.
