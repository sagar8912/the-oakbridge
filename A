The repository audit is complete. Do NOT redesign the architecture.

The audit confirms that the application already has the shared authorization foundation:

* packages/authorization
* permission_catalog
* user_permission_grants
* role_default_permissions
* permission_rules
* permission_audit_log
* kb_access_grants
* item_contributors / goal_contributors
* existing SQL PDP/PEP flow

It also confirms that kb_user_feature_permissions is a second/duplicate L0 permission mechanism and should eventually be removed.

We are now implementing the architecture in controlled migration waves.

IMPORTANT:
Implement ONLY WAVE 0 in this task.

Do not implement Wave 1, Wave 2, entity_relationships, RLS changes, or frontend changes yet.

WAVE 0 — FREEZE AND NORMALIZE THE PERMISSION CATALOG

Goal:

Make the existing permission catalog the canonical vocabulary for Knowledge Base permissions without creating a new permission system.

First inspect:

* permission_catalog
* user_permission_grants
* kb_user_feature_permissions
* kb_access_grants
* existing KB feature IDs
* existing permission keys
* all usages of kb_user_feature_permissions
* all usages of permission_catalog
* all usages of user_permission_grants

Create a complete mapping from existing KB feature IDs to canonical permission_catalog keys.

Examples may include concepts such as:

kb.article.edit
kb.article.review
kb.article.delete

BUT do not invent permission names if equivalent keys already exist.

Use the project’s existing naming convention.

SCOPE MODEL

Extend the existing user_permission_grants scope model only if required.

The supported scope dimensions should ultimately be able to distinguish:

* global
* knowledge_base
* hierarchy_node
* article
* content_type

Do not create separate permission tables for these scopes.

If the current database already has scope_type/scope_id, extend it rather than creating another grant table.

DATA SAFETY

Before changing anything:

1. Identify all rows in kb_user_feature_permissions.
2. Identify all rows in user_permission_grants.
3. Identify all existing permission_catalog entries.
4. Identify all KB feature IDs.
5. Produce a deterministic mapping between the old feature permissions and canonical permission keys.
6. Identify any unmapped or ambiguous permissions.

Do NOT delete kb_user_feature_permissions yet.

Do NOT migrate/drop data unless the mapping is complete and deterministic.

Create the migration required for Wave 0 only.

The migration must:

* be reversible
* preserve existing data
* avoid duplicate grants
* preserve existing permission behavior
* follow the existing migration conventions
* include appropriate constraints/indexes

TESTS

Add database tests proving:

1. Existing permission_catalog keys remain valid.
2. Existing user_permission_grants remain valid.
3. Scope types are validated.
4. Duplicate grants cannot be introduced incorrectly.
5. Existing KB permissions still resolve.
6. Existing users retain their current access.
7. Existing kb_user_feature_permissions data is untouched in this wave.

IMPORTANT ARCHITECTURAL RULES

Do NOT create:

* a new PDP
* a new PEP
* a new permission engine
* kb_user_roles
* kb_role_assignments
* content_type_permissions
* another user override table
* another contributor table
* another entity graph
* another authorization package

Reuse the existing packages/authorization implementation.

Do not change frontend behavior.

Do not change RLS.

Do not create entity_relationships yet.

Do not remove kb_user_feature_permissions yet.

At the end, report:

A. Files changed
B. Migration created
C. Permission catalog mappings
D. Scope changes
E. Tests added
F. Existing data affected
G. Rollback procedure
H. Any ambiguous mappings that require human review

If any mapping is ambiguous, STOP before destructive changes and report the ambiguity instead of guessing.

Do not commit anything.
