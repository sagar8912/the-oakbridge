Do not create a new Python MCP server.

After reviewing the repository, I understand we already have an MCP implementation using the official TypeScript MCP SDK inside packages/mcp-kb-admin.

I want to extend the existing MCP implementation instead of creating a parallel stack.

Please do the following:

1. Analyze the existing MCP package.
2. Explain the current architecture.
3. List every MCP tool already registered.
4. Compare them against John's requirements.

John's expectations are:

- Knowledge Base exposed through MCP
- Rich tool metadata
- AI agent discoverability
- LangSmith tracing
- Structured JSON outputs
- Production-ready tool registration

Then identify:

- What is already implemented.
- What is missing.
- What files need modification.
- What should be added without breaking the current architecture.

Do not generate code yet.

Generate an implementation plan first
