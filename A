Do not modify any application code yet.

We need to verify Wave 2 against our existing STAGING Supabase environment.

The previous test attempt tried to connect to 127.0.0.1:54322, which is incorrect for this project.

Please inspect the repository ONLY to determine:

1. How this project normally connects to the staging Supabase database.
2. Which environment variables/configuration are used for staging.
3. Which existing test commands are intended to run against staging.
4. Whether the Wave 2 SQL/DML tests can safely run against staging without destructive operations.
5. Why the previous test command attempted to connect to localhost:54322.

Do NOT:
- start local Supabase
- start Docker
- create a local PostgreSQL instance
- modify application configuration
- modify .env files
- change Wave 2 implementation
- create migrations
- reset/truncate/drop staging data
- start Wave 3
- commit anything

If the repository requires a specific environment variable or test command for staging, tell me the exact command/configuration needed, but do not expose any secret values.

Then report:

A. Existing staging connection mechanism
B. Correct test command(s)
C. Why the previous command used localhost
D. Whether Wave 2 tests are safe to execute against staging
E. Any test that must NOT be run against staging because it is destructive

STOP after this investigation. Do not run the tests yet and do not change files.
