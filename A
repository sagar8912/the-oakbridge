Act as a Principal Full-Stack Engineer / Solution Architect.

We are continuing the Knowledge Base (KB Admin) implementation discussed with John.

CURRENT STATUS:

- Phase A — Stabilization / Dead-Code Cleanup: COMPLETE
- Phase B — Schema / Content Foundation: LOCAL IMPLEMENTATION COMPLETE
- Phase C — Permissions / USER + TEAM Access Foundation: LOCAL IMPLEMENTATION COMPLETE
- Phase D — Content Lifecycle / Authoring / Approval / Publishing Foundation: LOCAL IMPLEMENTATION COMPLETE
- Phase E — GitHub / CI-CD → Knowledge Article Generation Foundation: LOCAL IMPLEMENTATION COMPLETE / READY TO CONTINUE

Some staging/integration validations and business decisions remain deferred.

IMPORTANT:

Do NOT block Phase F local implementation on staging, GitHub secrets, merge, deployment, or John’s unresolved Phase E questions.

Do NOT guess unresolved business rules.

Proceed with:

PHASE F ONLY — KNOWLEDGE RETRIEVAL + MCP / AGENT INTEGRATION FOUNDATION

==================================================
BUSINESS GOAL FROM JOHN
==================================================

John wants the Knowledge Base to become the authoritative knowledge source that future agents can query.

His example was:

Instead of IT/HR/LangSmith agents referencing:

- Word documents
- PDFs
- shared-drive documents
- disconnected files

they should eventually reference the corresponding approved/published knowledge stored in the Knowledge Base.

The architecture should support:

Knowledge Sources
        ↓
KB Articles
        ↓
Hierarchy + Permissions + Lifecycle
        ↓
Retrieval Layer
        ↓
MCP / Callable API
        ↓
LangSmith / AI Agents

This phase is FOUNDATION ONLY.

Do NOT migrate a production IT/HR agent yet.

==================================================
STEP F1 — AUDIT EXISTING RETRIEVAL ARCHITECTURE
==================================================

Before modifying code, inspect the repository thoroughly.

Audit:

- existing KB search
- article retrieval APIs
- hierarchy APIs
- MCP servers/tools
- KB MCP implementation
- search adapters
- database search functions
- full-text search
- embeddings/vector search if present
- LangSmith references
- agent integration code
- authentication
- API-key authentication
- PDP/PEP/RLS enforcement
- existing docs/research/plans related to retrieval/MCP

Identify what already exists.

DO NOT create duplicate architecture if repository functionality already exists.

Report the existing retrieval flow before implementing changes.

==================================================
STEP F2 — DEFINE CANONICAL RETRIEVAL CONTRACT
==================================================

Create or align a canonical retrieval contract suitable for:

1. Human KB search
2. MCP tools
3. Future LangSmith agents
4. Other model-agnostic agent clients

The contract should support, where appropriate:

- query
- hierarchy scope
- article/content type
- status
- tags/categories
- result limit
- permission context

Returned results should contain only useful fields such as:

- article/content ID
- title
- summary/snippet
- relevant content
- hierarchy/location
- source/reference metadata where appropriate
- updated/published metadata
- stable reference for retrieving full content

Do not expose internal secrets or unnecessary private metadata.

==================================================
STEP F3 — PUBLISHED/AUTHORIZED CONTENT ONLY
==================================================

This is critical.

Normal agent retrieval must NOT automatically expose:

- drafts
- rejected content
- archived/inactive content where existing lifecycle says it should be hidden
- content the caller cannot access

Integrate retrieval with Phase C + Phase D.

Expected conceptual flow:

Agent/User Query
        ↓
Authentication
        ↓
Permission Scope
        ↓
Eligible Published Knowledge
        ↓
Search / Retrieval
        ↓
Authorized Results Only

Do not implement search first and filter sensitive results afterward if that could leak unauthorized information.

Enforce access at the appropriate boundary.

==================================================
STEP F4 — HIERARCHY-AWARE RETRIEVAL
==================================================

Use the KB hierarchy foundation.

Support retrieval scoped appropriately by:

- global/all-accessible knowledge
- hierarchy node
- child/descendant scope only if existing architecture supports it safely
- USER/TEAM grants where implemented

Do NOT invent unresolved USER/TEAM inheritance behavior.

If inheritance rules are still awaiting John’s decision:

preserve the current safe semantics and document the limitation.

Do not broaden access.

==================================================
STEP F5 — SEARCH QUALITY FOUNDATION
==================================================

Inspect what search capability currently exists.

Prefer reuse before replacement.

Support a stable baseline search using repository-supported mechanisms.

Possible mechanisms:

- PostgreSQL text search
- keyword search
- existing search functions
- existing vector/embedding infrastructure

Do NOT introduce a new vector database or AI vendor unless clearly justified by existing approved architecture.

The retrieval architecture should allow future enhancement without rewriting the client/MCP contract.

Separate:

RETRIEVAL CONTRACT

from:

SEARCH IMPLEMENTATION.

==================================================
STEP F6 — MCP TOOLS
==================================================

Audit existing KB MCP tools first.

Reuse or safely extend existing tools rather than creating duplicates.

The MCP layer should provide a minimal useful read-only capability such as:

KB_SEARCH

Purpose:
Search authorized published KB content.

KB_ARTICLE_GET

Purpose:
Retrieve a specific authorized article/content record.

KB_HIERARCHY

Purpose:
Discover navigable knowledge structure if already supported/required.

Use existing repository naming conventions if equivalent tools already exist.

DO NOT create duplicate tool names simply because these names are suggested.

==================================================
STEP F7 — MCP SECURITY
==================================================

MCP must NOT bypass the application security model.

Validate:

- API authentication
- service authentication where applicable
- user/actor context where supported
- hierarchy access
- USER/TEAM grants
- publication state

Test explicitly:

Authorized request
→ allowed authorized content.

Unauthorized request
→ rejected or zero inaccessible results.

Draft content
→ unavailable to normal retrieval.

Restricted content
→ unavailable without permission.

Do not weaken RLS/PDP/PEP.

==================================================
STEP F8 — AGENT-FRIENDLY RESPONSE DESIGN
==================================================

Design retrieval responses so an agent can reliably ground an answer.

Where supported, include stable provenance such as:

- KB article/content ID
- title
- hierarchy
- canonical internal reference
- last updated/published information

The goal is to enable future agents to answer:

“According to which knowledge article?”

Avoid returning huge unbounded content payloads when a search result + detail retrieval pattern is more appropriate.

==================================================
STEP F9 — MODEL-AGNOSTIC BOUNDARY
==================================================

John explicitly wants the architecture model-agnostic.

The KB retrieval layer must NOT depend directly on:

- one LLM provider
- one agent framework
- one LangSmith implementation

Architecture:

KB
   ↓
Retrieval Service/API
   ↓
MCP / Callable Contract
   ↓
Any authorized client
      ├── LangSmith
      ├── future agents
      ├── Cursor/Claude-style workflows where appropriate
      └── other model clients

LangSmith should be a CONSUMER, not the owner of KB retrieval logic.

==================================================
STEP F10 — FUTURE IT/HR AGENT MIGRATION CONTRACT
==================================================

Prepare the integration seam for John’s stated use case.

Current conceptual state:

IT/HR Agent
    ↓
Word/PDF/Shared Drive

Future:

IT/HR Agent
    ↓
KB MCP / Retrieval API
    ↓
Published + Authorized Knowledge Base

DO NOT migrate production agents in Phase F.

Instead:

- document the adapter/interface
- provide a local example/test consumer if useful
- identify what a future migration would replace
- identify configuration required

Do not hardcode production LangSmith credentials.

==================================================
STEP F11 — LOCAL TEST MATRIX
==================================================

Implement and run local tests for at least:

1. Published accessible article
   → returned by search.

2. Draft article
   → not returned to normal agent retrieval.

3. Restricted article without access
   → not returned.

4. Restricted article with valid access
   → returned.

5. Article detail retrieval without permission
   → denied/not found safely.

6. Hierarchy-scoped query
   → only permitted scope returned.

7. Empty/no-match search
   → safe empty result.

8. Invalid authentication
   → rejected.

9. MCP search
   → same authorization semantics as direct retrieval API.

10. MCP article get
    → cannot bypass permission rules.

11. Existing TOW/KB functionality
    → no regression.

==================================================
STEP F12 — OBSERVABILITY
==================================================

Add/reuse appropriate structured diagnostics for:

- retrieval request
- retrieval failure
- MCP failure
- authorization denial
- search timing where appropriate

Do NOT log:

- secrets
- API keys
- tokens
- unnecessary sensitive article content

==================================================
STEP F13 — REGRESSION
==================================================

Run all locally available:

- typecheck
- lint
- unit tests
- integration tests
- build

Verify no regressions to:

- TOW
- KB UI
- authentication
- hierarchy
- permissions
- article lifecycle
- Phase E generation foundation
- existing MCP tools

Fix only issues caused by Phase F.

==================================================
STRICT SCOPE
==================================================

DO NOT implement the next phase.

Specifically do NOT yet:

- migrate the real IT ticketing agent
- migrate the HR agent
- bulk import Word/PDF/shared-drive documents
- introduce a new LLM provider
- create a new vector DB unless already explicitly required
- change unresolved USER/TEAM inheritance rules
- automatically publish content
- modify unrelated TOW features

DO NOT:

- commit
- push
- merge
- create PR
- deploy
- apply staging/production changes

==================================================
FINAL REPORT
==================================================

Return:

PHASE F STATUS:
COMPLETE / PARTIALLY COMPLETE / BLOCKED

Then report:

1. Existing retrieval architecture found
2. Files/components changed
3. Canonical retrieval contract
4. Search implementation used
5. Hierarchy behavior
6. Permission/security enforcement
7. MCP tools created/reused/modified
8. Agent integration boundary
9. IT/HR future migration seam
10. Tests executed and exact results
11. Regression status
12. Deferred staging/integration work

Clearly separate:

LOCAL IMPLEMENTATION COMPLETE

from:

DEFERRED INTEGRATION/STAGING VALIDATION

List:

QUESTIONS FOR JOHN

only where a genuine business decision is required.

Finally answer:

READY FOR NEXT LOCAL PHASE: YES/NO

STOP after Phase F.

Do not begin the next phase.
Do not commit.
Do not push.
Do not merge.
Do not deploy.
