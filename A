Continue with PHASE B ONLY — do not start Phase C.

Current status:
PHASE B PARTIALLY COMPLETE.

The T1-05/T1-06 migrations and DocumentManager/ERD alignment are implemented, but Phase B must be fully validated before we move to permissions/PDP-PEP.

IMPORTANT:
- Do NOT start Phase C.
- Do NOT invent USER/TEAM inheritance rules.
- Do NOT make unrelated TOW changes.
- Do NOT expand scope beyond the approved Phase B architecture.
- Do NOT commit or push unless explicitly instructed.
- Do NOT expose or print secrets.

STEP 1 — REVIEW PHASE B CHANGES

Re-audit all Phase B changes already made.

Confirm:
- T1-05 migration is correct and dependency-safe.
- T1-06 migration is correct and dependency-safe.
- Migration ordering is correct.
- Existing production/staging data is preserved.
- No destructive migration exists unless explicitly required by the approved repo plan.
- DocumentManager matches the intended schema.
- ERD/docs match the actual implementation.

Report any mismatch before proceeding.

STEP 2 — DETERMINE SAFE VALIDATION PATH

Determine whether the current environment is connected to:

LOCAL
STAGING
or PRODUCTION.

Never apply unverified migrations to production.

If a safe staging/local database is available and credentials/configuration are already correctly configured:

Apply T1-05/T1-06 using the repository's existing migration mechanism.

Do not invent a new migration process.

If migration application is blocked because code must first be merged/deployed or because GitHub integration is required:

DO NOT bypass that process.

Instead report the exact blocker and the exact manual action required.

STEP 3 — DATABASE VALIDATION

After migration application, verify:

- expected tables/columns/constraints exist
- foreign keys are correct
- indexes are present where specified
- existing records remain intact
- article/content/version relationships work
- hierarchy relationships work
- no duplicate or orphaned records were introduced

STEP 4 — CORE KB SMOKE TEST

Validate existing supported KB functionality against the updated schema:

1. KB application loads.
2. Hierarchy/navigation loads.
3. Existing article list loads.
4. Existing article opens correctly.
5. Existing content renders correctly.
6. Existing supported create/edit flow still works where currently implemented.
7. Existing approval/publish behavior still works where currently implemented.
8. Search/retrieval still works where currently supported.
9. Existing TOW → KB deep links/scaffolding still work.
10. No new server/browser errors are introduced.

STEP 5 — AUTOMATED VALIDATION

Run all available relevant:

- unit tests
- integration tests
- type checks
- lint
- build
- migration/schema validation

If local automated validation is blocked by environment/tooling limitations, clearly separate:

CODE FAILURE

from

ENVIRONMENT/EXECUTION BLOCKER.

Do not mark Phase B failed merely because this machine cannot execute an external integration.

STEP 6 — PHASE B ACCEPTANCE

Compare the implementation against the existing repository Track/T1-05/T1-06 acceptance criteria.

Do not use Phase C permission requirements as Phase B acceptance criteria.

USER/TEAM permission inheritance must remain deferred until Phase C and must not be invented.

FINAL REPORT

Return exactly:

PHASE B STATUS:
COMPLETE
PARTIALLY COMPLETE
or BLOCKED

Then provide:

1. T1-05 status
2. T1-06 status
3. Migration application status
4. Database validation result
5. DocumentManager/ERD alignment result
6. Automated test/build results
7. KB smoke/UAT results
8. TOW → KB regression result
9. Remaining blockers, if any
10. Exact manual action required, if any

Finally answer:

CAN PHASE B BE CLOSED? YES/NO

If YES:
State that Phase C may be planned next, but DO NOT start it.

If NO:
Give only the minimum actions necessary to close Phase B.

STOP after this validation.
Do not start Phase C.
Do not commit or push.
