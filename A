Do not migrate to Resend or any other email provider.

Keep Microsoft 365 (Office 365) SMTP as the production email provider.

Investigate why password recovery emails are not delivered.

Current state:
- SMTP Host: smtp.office365.com
- Port: 587
- Custom SMTP enabled in Supabase
- Username and Sender are the same mailbox.
- POST /recover sometimes returns 504 (Context deadline exceeded), then eventually returns 200.
- Supabase Auth logs show user_recovery_requested.
- No password reset email is received.

Tasks:
1. Verify SMTP authentication succeeds.
2. Verify STARTTLS negotiation.
3. Verify Exchange accepts the message.
4. Check SMTP response codes.
5. Check whether the SMTP server times out.
6. Create a standalone SMTP diagnostic using the same credentials.
7. Verify Send As permissions if required.
8. Verify mailbox SMTP AUTH is enabled.
9. Verify Conditional Access or Security Defaults are not blocking SMTP.
10. Keep Microsoft 365 SMTP and identify the exact root cause with evidence instead of changing providers.
