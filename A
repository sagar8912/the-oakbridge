Proceed with Phase 2.

Do not change the existing architecture.

Goal:
Upgrade the existing MCP tools to return structured JSON responses while remaining fully backward compatible.

Requirements:

1. Modify the existing tools:
   - kb_search
   - kb_article_get
   - kb_hierarchy
   - kb_query
   - kb_tools_list
   - kb_tool_invoke

2. Reuse the existing formatting utilities from packages/mcp-shared wherever possible.

3. Every tool should return:
   {
     success: boolean,
     tool: string,
     timestamp: ISO8601,
     data: ...,
     metadata: ...
   }

4. Preserve the current human-readable output if required by existing clients, but expose the structured JSON for MCP consumers.

5. Do not modify any backend APIs.

6. Do not create duplicate logic.

7. Update tests if needed.

After implementation, summarize:
- Files changed
- JSON response format
- Backward compatibility considerations
