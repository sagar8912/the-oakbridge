
Act as a Principal AI + RAG + LangSmith Engineer.

After the recent LangSmith integration, the Knowledge Assistant is no longer answering questions correctly.

Current issue:
- Every question returns:
  "I couldn't find information in the Knowledge Base."
- This happened only after the LangSmith changes.
- Anthropic API is configured and connected.
- Retrieval Dashboard shows indexed articles and indexed chunks.
- MCP tools are registered.
- The chatbot was working correctly before the LangSmith changes.

Your task is to find and fix the root cause instead of adding temporary workarounds.

Perform a complete end-to-end audit of the chatbot pipeline:

1. User Query
   - Verify the user question reaches the backend correctly.

2. Retrieval
   - Verify SearchKnowledge executes.
   - Verify SearchDocuments executes.
   - Verify SearchHierarchy executes.
   - Verify SearchGitHub executes when applicable.
   - Verify retrieved chunks are not empty.
   - Log retrieved article IDs and chunk count.

3. MCP
   - Ensure every MCP tool is executing successfully.
   - If a tool throws an error, continue with remaining tools.
   - Do not allow one MCP failure to stop the pipeline.

4. LangSmith
   - Verify LangSmith tracing is NON-BLOCKING.
   - If LangSmith is unavailable or throws an exception:
       - Continue retrieval.
       - Continue Anthropic response generation.
       - Log the tracing error only.
   - LangSmith must never prevent answers from being generated.

5. Anthropic
   - Verify retrieved context is actually passed to Claude.
   - Verify the final prompt contains:
       - System prompt
       - Retrieved context
       - User question

6. Response Generation
   - Only return
     "I couldn't find information in the Knowledge Base."
     if ALL retrieval sources return zero relevant documents.
   - Otherwise generate an answer using retrieved evidence.

7. Logging
   Add detailed debug logs for:
   - User question
   - Retrieved article count
   - Retrieved chunk count
   - MCP tool results
   - Final prompt size
   - Anthropic request
   - Anthropic response
   - LangSmith trace status
   - Any exception

8. Fail-safe
   If LangSmith fails:
   - Disable tracing automatically for that request.
   - Continue normal chatbot execution.
   - Never fail the request because of tracing.

9. Validation
   Test these questions:
   - What is Mission?
   - Explain Goals.
   - What is Vision?
   - What is Workflow Test?
   - What is Technical Documents?
   - How do I create a Goal?
   - What is GitHub Integration?
   - What documents are uploaded?
   - Explain TOW App.
   - Explain KB Users.

For every test show:
- Retrieval succeeded? (Yes/No)
- Number of retrieved chunks
- MCP tools executed
- LangSmith trace created? (Yes/No)
- Anthropic request succeeded? (Yes/No)
- Final answer generated? (Yes/No)

Do not reduce chatbot functionality.
Do not remove LangSmith.
Fix the integration so Retrieval, MCP, Anthropic, and LangSmith all work together.
