Do NOT make any code changes.

I am currently viewing the KB Admin Permission Management drawer for a user.

The UI shows:
- Effective permissions such as Read/Create/Edit/Approve/Manage
- Explicit Grants (User)
- Inherited Grants (Team)
- Feature permissions marked INHERITED
- Administration permissions marked INHERITED
- Knowledge Base scope

For this exact UI, tell me:

1. Which API call supplies the effective permission values?
2. Which API supplies explicit grants?
3. Which API supplies inherited/team grants?
4. Where does the UI get the permission source?
5. Which centralized evaluator is responsible for the final ALLOW/DENY?
6. Does the UI itself calculate inheritance, or does the backend/evaluator return it?
7. What is the safest existing test user and permission I can use to verify an explicit ALLOW without modifying production/Staging data?

Read-only inspection only. Do not modify code, database, migrations, commit, or create a PR.
