
Perform a root cause analysis for why kb_list_categories returns:

"I could not find information in the Knowledge Base... it does not contain a list of Knowledge Base categories."

Do NOT rewrite the implementation immediately.

Investigate the entire flow step by step.

1. Verify the MCP server is invoking kb_list_categories.
2. Verify kb_list_categories is calling the correct endpoint (/api/retrieval/hierarchy).
3. Log the raw HTTP response from the endpoint.
4. Check whether the endpoint actually returns category data.
5. Verify the flattening/transformation logic maps the response correctly.
6. Verify the JSON envelope contains:
   - success
   - tool
   - timestamp
   - data.categories
   - metadata
7. Check whether the Knowledge Assistant ignores or filters the returned categories.
8. Compare the raw API response with the final tool response and identify where data is lost.
9. If the endpoint itself returns no categories, identify why (empty database, wrong URL, missing auth, incorrect environment, etc.).
10. If code changes are required, make only the minimal fix required. Do not change the architecture or backend APIs.

Finally provide:
- Root cause
- Files inspected
- Files modified (if any)
- Before vs After behavior
- Example successful kb_list_categories response
