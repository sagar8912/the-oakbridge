
Act as a Principal AI + RAG + Analytics Engineer.

BUG:

The Retrieval Dashboard analytics are incorrect.

Current behavior:
- The Knowledge Assistant returns:
  "I couldn't find information in the Knowledge Base."
- However, "Failed Searches" remains 0.
- AI Success Rate is still 100%.

Expected behavior:
Whenever Retrieval returns zero relevant documents or the assistant responds with:
"I couldn't find information in the Knowledge Base."
the system must:

1. Record a Failed Search event.
2. Increment Failed Searches.
3. Recalculate AI Success Rate.
4. Store:
   - question
   - user
   - timestamp
   - retrieval mode
   - retrieval tools executed
   - retrieved chunk count
   - reason for failure

Audit the entire analytics pipeline.

Verify:
- SearchKnowledge
- SearchDocuments
- SearchHierarchy
- SearchGitHub
- Analytics event creation
- Dashboard query
- Database persistence

Fix any missing analytics writes.

AI Success Rate must be calculated as:
Successful Answers / Total Questions × 100

Example:
Question 1 -> success
Question 2 -> success
Question 3 -> no evidence

Dashboard should display:
AI Success Rate = 66.7%
Failed Searches = 1

Add logging to show:
- Retrieved chunk count
- Success/Failure
- Analytics event written
- Updated dashboard values

After fixing, automatically run these tests:

1. "What is Mission?" (Expected: Success)
2. "Generate PDF" (Expected: Failed Search)
3. "Random Unknown Question" (Expected: Failed Search)

Verify that:
- Failed Searches increments correctly.
- AI Success Rate updates correctly.
- Dashboard reflects live analytics without refresh issues.
