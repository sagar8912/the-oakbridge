Now integrate the MCP Wrapper with the existing Knowledge Base APIs.

Requirements:

- search_knowledge_base() should call the existing search endpoint.
- get_article() should call the existing article endpoint.
- list_categories() should call the category service.

Do not duplicate business logic.

Use dependency injection so the MCP layer only delegates requests to the existing services.

Implement robust exception handling.

Return standard MCP responses.

Create unit tests.

Ensure everything follows SOLID principles.
