
Act as a Principal Full-Stack Engineer.

The current GitHub Integration UI asks administrators to manually enter:

- Knowledge Base ID (UUID)
- Hierarchy Node ID (UUID)

This is not user friendly.

Replace this implementation with a navigation-based selector that matches the existing Knowledge navigation tree.

Requirements

1. Remove the manual UUID input fields from the UI.

Remove:
- Knowledge Base ID textbox
- Hierarchy Node ID textbox

These values should still exist internally but must never be manually entered.

--------------------------------------------------

2. Build an "Import Into" selector.

Use the same hierarchy already shown in the Knowledge sidebar.

Example:

Client Lifecycle
    Submission & Quoting
    Sales
    Claims

TOW App
    OOO Items
        Mission
        Purpose
        Vision
        Goals

Technical Documents

Data & Analytics

The Oakbridge Way

--------------------------------------------------

3. Allow selecting any destination node.

Examples:

Mission

Purpose

Vision

Goals

Getting Started

Technical Documents

etc.

--------------------------------------------------

4. When a node is selected:

Automatically resolve:

- knowledge_base_id
- hierarchy_node_id

These values should be looked up internally from the database.

Do NOT require the admin to know UUIDs.

--------------------------------------------------

5. Existing GitHub Import logic must continue working.

The PR import API should receive exactly the same IDs it currently expects.

Only the UI changes.

--------------------------------------------------

6. Display selected destination.

Example:

Import Destination

TOW App
→ OOO Items
→ Mission

instead of UUIDs.

--------------------------------------------------

7. Repository dropdown remains unchanged.

Repository selection continues to work exactly as today.

--------------------------------------------------

8. Validation

Disable Import until:

✓ Repository selected

✓ Destination node selected

--------------------------------------------------

9. Keep compatibility.

Do not break:

- Import History
- Pull Requests
- Sync Status
- GitHub webhook
- Existing backend APIs

--------------------------------------------------

10. Backend

If APIs currently require UUIDs:

Create an internal resolver:

navigation path
↓

hierarchy node

↓

knowledge_base_id

↓

hierarchy_node_id

↓

existing import API

No API contract should change.

--------------------------------------------------

11. UX improvements

Add:

• Search node

• Expand/Collapse tree

• Breadcrumb preview

Example:

Importing into

TOW App
→ OOO Items
→ Mission

--------------------------------------------------

12. Testing

Verify:

✓ No UUID input visible

✓ Tree matches Knowledge navigation

✓ Correct node selected

✓ Correct UUIDs resolved internally

✓ Existing import still works

✓ Existing APIs unchanged

At the end provide:

1. Files modified
2. Components added
3. APIs reused
4. How UUIDs are resolved internally
5. Before vs After UI screenshots/description
