Update the existing HTML/PPT content and remove the incorrect "KB Super Admin" terminology completely.

IMPORTANT:
Do NOT use "Super Admin" anywhere in the presentation.
The correct permission/role model should use only these three KB roles:

1. KB Admin
2. KB Editor
3. KB Reader / Viewer

Use these three roles consistently throughout the entire presentation.

ROLE DEFINITIONS:

KB Admin
- Has administrative control over the Knowledge Base.
- Can manage KB users/roles and permissions.
- Can access Permission Management.
- Can manage feature-level permissions.
- Can manage content-wise permissions.

KB Editor
- Can work with/edit KB content according to the permissions assigned to them.
- Their access should be controlled through the configured feature and content permissions.
- Do not describe them as an administrator.

KB Reader / Viewer
- Read/view-only access to permitted KB content.
- Cannot be described as an editor or administrator.
- Use "KB Reader / Viewer" consistently wherever the view/read-only role is referenced.

IMPORTANT CORRECTION:
Do NOT show:
- KB Super Admin
- Super Admin
- Generic KB User as a primary role
- Any other role that is not part of the three-role model above.

The presentation should explain the permission model using ONLY:
KB Admin → KB Editor → KB Reader / Viewer

UPDATE THE FIRST SLIDES:

Slide 1:
Title: "KB Roles & Permissions"

Show the three roles visually:

KB Admin
↓
KB Editor
↓
KB Reader / Viewer

However, do NOT imply that this is necessarily a strict hierarchy unless supported by the existing product behavior. Present them as the three defined KB roles.

Add a concise explanation of each role.

Slide 2:
Title: "KB Role & Access Model"

Create a clean visual showing:

KB Admin
- Manage users
- Manage permissions
- Manage KB access

KB Editor
- Create/Edit content where permitted
- Access assigned features
- Access assigned content

KB Reader / Viewer
- View permitted content
- Read-only access

IMPORTANT:
Only include "Create/Edit content" for KB Editor if this is supported by the existing application. Otherwise phrase it as "Edit assigned content".

PERMISSION MANAGEMENT FLOW:

Update the existing Permission Management section to use:

KB Admin
   ↓
Permission Management
   ↓
Select KB Role / User
   ↓
Manage Feature Permissions
   ↓
Manage Content-wise Permissions
   ↓
Apply Access

Do NOT mention a Super Admin anywhere.

CONTENT-WISE PERMISSION FLOW:

KB Admin
   ↓
Permission Management
   ↓
Content Management
   ↓
Select KB Editor / KB Reader-Viewer
   ↓
Configure permitted content
   ↓
Apply access

Again, do not invent unsupported permission types.

DEFAULT ACCESS:

If the existing application/product behavior confirms default Reader/Viewer access, explain it as:

"Users without additional assigned permissions receive the applicable Reader / Viewer level of access."

Do not claim that every newly created user automatically receives both Reader AND Viewer unless this is confirmed in the actual product.

VERY IMPORTANT:
The presentation should distinguish between:

ROLE
- KB Admin
- KB Editor
- KB Reader / Viewer

FEATURE PERMISSIONS
- What features/actions the role can access

CONTENT PERMISSIONS
- What KB content/resources the user can access

Do not mix these three concepts.

UPDATE THE FINAL END-TO-END DIAGRAM:

KB Admin
   ↓
User / Role Assignment
   ↓
KB Editor OR KB Reader / Viewer
   ↓
Permission Management
   ↓
Feature Permissions
   ↓
Content-wise Permissions
   ↓
Final User Access

PRESENTATION STRUCTURE SHOULD NOW BE:

Slide 1 — KB Roles & Permissions
Slide 2 — KB Role & Access Model
Slide 3 — Permission Management
Slide 4 — Permission Management Flow
Slide 5 — Content-wise Permissions
Slide 6 — Content Permission Flow
Slide 7 — End-to-End Permission Workflow

Keep it client-ready, professional and visually clean.

MOST IMPORTANT:
Before generating the updated HTML/PPT, search the existing content and replace ALL occurrences of:
"KB Super Admin"
"Super Admin"
"KB User"

with the correct terminology wherever appropriate.

Do not simply rename the words. Rework the surrounding explanation so that the permission model logically represents:

KB Admin
KB Editor
KB Reader / Viewer

The final presentation must look like an intentional product permission model, not like an edited version of the previous incorrect model.
