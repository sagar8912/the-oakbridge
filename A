Act as a Senior Full-Stack Engineer.

The endpoint

/api/admin/session-kb-access

returns a 404.

Please investigate why.

Tasks:

1. Search the entire repository for:
   - session-kb-access
   - resolveKbPermissions
   - kb_access_grants
   - session permission API

2. Determine whether:
   - the route was renamed,
   - the route was deleted,
   - the frontend is calling the wrong URL,
   - or the API was never implemented.

3. If the route should exist:
   - implement or restore it,
   - register it correctly in the Next.js App Router,
   - ensure it returns the current session's effective KB permissions.

4. If the route has been renamed:
   - update every frontend caller to use the correct endpoint.

5. Verify:
   - GET /api/admin/session-kb-access returns HTTP 200.
   - Response contains:
     - systemRole
     - kbRole
     - effective feature permissions
     - KB scope
     - team grants
   - No 404 occurs.

6. Do not modify unrelated functionality.

7. Do not commit, merge, push, or deploy.

Finally report:
- Root cause.
- Files modified.
- Correct endpoint URL.
