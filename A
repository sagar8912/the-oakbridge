Wave 3 implementation is complete.

Do NOT make any further code changes.

Do NOT start Wave 4.

Do NOT apply any migration to Staging.
Do NOT run any SQL/DML against Staging.
Do NOT run pg_prove against Staging.
Do NOT run supabase test db.
Do NOT attempt to create or modify a local Supabase/Postgres instance.

Now perform ONLY SAFE VERIFICATION of the existing Wave 3 implementation.

Run all available tests that do NOT require a live database, including:

1. Authorization Vitest tests
2. Wave 1 tests
3. Wave 2 tests
4. Wave 3 unit/mock tests
5. KB Admin/OOU mocked tests
6. TypeScript type-check
7. ESLint/lint if configured
8. Frontend tests if configured
9. Build checks if safe
10. Static/reference checks for old permission paths

If any test attempts to connect to Supabase, Staging Postgres, or execute SQL/DML, SKIP that test and clearly report it as skipped.

IMPORTANT:
- Do not modify application code just to make tests pass.
- Do not modify migrations.
- Do not regenerate DB types against Staging.
- Do not change architecture.
- Do not refactor unrelated code.
- Do not commit anything.

After running the safe verification, give me exactly:

A. Tests PASSED
B. Tests FAILED
C. Tests SKIPPED because they require Staging/Supabase
D. TypeScript status
E. Lint status
F. Build status
G. Remaining kb_user_feature_permissions references
H. Remaining architectural concerns
I. Whether Wave 3 is ready to open as a PR

For the architectural concerns already identified, do not treat these as failures unless they actually break the implementation:
- New RPC will not exist in Staging until the migration is merged/applied.
- SQL catalog keys and TypeScript catalog keys must remain aligned.
- kb_effective_hierarchy_role remains for UI/grant display.
- Generated DB types do not yet contain the new RPC.

Stop after this verification report.
