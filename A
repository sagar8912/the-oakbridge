
You are working on the Oakbridge / OIL Console application.

I need to implement a new centralized permission and entity-relationship architecture based on the following design principles:

1. One shared back-end, never duplicated.
2. Shared-first capabilities.
3. One universal entity graph.
4. One record-role/contributor model for permissions.
5. One layered permission vocabulary:
    * Level 0: explicit user permission override
    * Level 1: system role
    * Level 2: application role
    * Level 3: record/entity-level permissions

The target design also includes Knowledge Base administration permissions such as:

* Per Knowledge Base
* Per Hierarchy Node
* Per Workflow
* Per Content Type

And entity relationships must be first-class, polymorphic relationships between entities.

IMPORTANT:
Do NOT modify code or database yet.

First perform a complete repository audit.

Inspect:

* frontend architecture
* backend architecture
* database schema
* migrations
* ORM/models
* existing authentication
* user/group/team handling
* existing RBAC/permissions
* Knowledge Base Admin implementation
* hierarchy implementation
* workflow implementation
* content type implementation
* entity implementation
* entity relationship implementation
* existing contributor/ownership tables
* authorization middleware/services
* API endpoints
* frontend permission guards
* tests

Search the entire repository for terms such as:
permission, permissions, role, roles, authorization, authorize, policy, PDP, PEP, user, group, team, contributor, entity, entity_relationship, knowledge_base, hierarchy, workflow, content_type, admin.

Do not assume table names.

Produce an implementation report containing:

A. Current architecture
B. Existing database tables relevant to permissions
C. Existing entity/entity_relationship tables
D. Existing KB Admin permission implementation
E. Existing authorization flow
F. Existing frontend permission checks
G. What can be reused
H. What must be changed
I. What should NOT be duplicated
J. Proposed migration strategy
K. Proposed API changes
L. Proposed frontend changes
M. Risks and backward compatibility concerns

Most importantly, identify whether the current system already has equivalent functionality before proposing new tables.

Do not create duplicate permission systems.

At the end, provide a concrete file-by-file implementation plan, but do not implement it yet.
