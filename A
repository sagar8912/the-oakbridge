Act as a Principal Full-Stack Engineer / Solution Architect.

We are continuing the Knowledge Base (KB Admin) implementation discussed with John.

CURRENT STATUS:

- Phase A — KB Stabilization / Dead-Code Cleanup: COMPLETE
- Phase B — Schema / Content Foundation: LOCAL IMPLEMENTATION COMPLETE; staging validation deferred
- Phase C — Permissions / USER + TEAM Access-Control Foundation: LOCAL IMPLEMENTATION COMPLETE; final staging/security validation deferred
- Phase D — Content Lifecycle / Authoring / Approval / Publishing Foundation: COMPLETE ENOUGH TO CONTINUE LOCALLY

IMPORTANT WORKFLOW RULE:

Do NOT:

- commit
- push
- create PR
- merge
- deploy
- apply staging/production migrations

We are completing local implementation phases first.

Proceed with:

PHASE E ONLY — GITHUB / CI-CD → KNOWLEDGE ARTICLE GENERATION PIPELINE

==================================================
JOHN'S REQUIREMENT
==================================================

John explained that development work already produces useful structured information through:

- GitHub PRs
- Markdown files
- PRDs
- implementation plans
- research documents
- feature documentation

His goal is that when a feature is developed and eventually released, we should not rely on tribal knowledge to explain the change to users.

The development/deployment workflow should be capable of producing a Knowledge Base article from the available development documentation.

Conceptually:

Feature / Requirement
        ↓
PRD / Markdown / Implementation Documentation
        ↓
Pull Request / Development Lifecycle
        ↓
Structured Knowledge Article Draft
        ↓
Review / Approval
        ↓
Published Knowledge Article

IMPORTANT:

Do NOT automatically publish generated content.

Generated knowledge must enter the controlled KB lifecycle created in Phase D.

==================================================
STEP E1 — AUDIT EXISTING GITHUB / CI-CD ARCHITECTURE
==================================================

Before modifying code, inspect:

- .github/workflows
- GitHub Actions
- PR templates
- markdown documentation
- docs/research
- docs/plans
- PRD locations
- deployment workflows
- staging/prod promotion workflows
- existing automation scripts
- GitHub API integrations
- webhooks if any
- KB APIs/MCP tools capable of creating content

Determine:

1. What information is available during a PR.

2. Where PRDs/implementation docs live.

3. How a PR relates to a feature.

4. What event currently represents release/deployment.

5. Whether any existing documentation automation exists.

6. Whether repository research/plans already define this pipeline.

Do not modify code until the audit is complete.

==================================================
STEP E2 — DEFINE CANONICAL KNOWLEDGE GENERATION CONTRACT
==================================================

Define a structured contract for converting development information into a KB article draft.

At minimum consider:

- title
- summary
- what changed
- why it changed
- who is affected
- how to use the feature
- user-visible behavior
- permissions/access considerations
- known limitations
- related feature/module
- source PR
- source documentation
- release/deployment reference
- generated timestamp

Do not blindly copy engineering documentation into the KB.

The output should be useful from an END-USER / BUSINESS USER perspective.

Preserve traceability back to source development artifacts.

==================================================
STEP E3 — SOURCE DISCOVERY
==================================================

Implement a deterministic way to discover relevant documentation associated with a PR/feature.

Potential sources include existing repository conventions such as:

- PR description
- linked PRD
- docs/research
- docs/plans
- implementation markdown
- changed documentation files

Do NOT invent fragile assumptions if the repository already defines conventions.

Prefer explicit references/metadata where available.

If no reliable association exists, report the gap instead of guessing unrelated documents.

==================================================
STEP E4 — ARTICLE DRAFT GENERATION
==================================================

Implement the minimum safe pipeline that can transform the discovered feature information into a structured:

KNOWLEDGE ARTICLE DRAFT

Important:

The generated result MUST NOT bypass Phase D.

It should enter the lifecycle as:

GENERATED CONTENT
        ↓
DRAFT
        ↓
HUMAN REVIEW
        ↓
APPROVAL
        ↓
PUBLISH

Never:

Generated
        ↓
Automatically Published

unless an existing explicitly approved business rule says otherwise.

==================================================
STEP E5 — IDEMPOTENCY / DUPLICATE PROTECTION
==================================================

Prevent the same PR/release from creating uncontrolled duplicate articles.

Use an appropriate stable source reference such as:

- repository
- PR number
- source identifier
- deployment/release identifier

consistent with existing architecture.

The pipeline should be capable of determining:

NEW SOURCE
→ create draft

EXISTING SOURCE
→ update/revise appropriate draft or safely no-op

Do not overwrite published authoritative content unsafely.

Respect Phase D revision/version behavior.

==================================================
STEP E6 — TRACEABILITY
==================================================

Every generated article should retain machine-readable traceability where architecture supports it.

Examples:

- source repository
- PR number
- source commit/SHA
- source document(s)
- generation method
- generation timestamp

This is important so we can answer:

“Why does this KB article exist?”

and:

“Which code/change generated it?”

Do not expose unnecessary internal engineering metadata to normal end users if it belongs only in audit metadata.

==================================================
STEP E7 — SECURITY / PERMISSIONS
==================================================

Integrate with Phase C and Phase D.

The automation identity/process must not become an unrestricted security bypass.

Ensure:

- article creation uses an explicitly authorized service/system path
- generated content is DRAFT
- publication still requires authorized lifecycle action
- generated articles respect hierarchy/access architecture
- direct unauthorized API calls remain rejected

Do not weaken existing RLS/PDP/PEP protections to make automation easier.

==================================================
STEP E8 — GITHUB ACTION / PIPELINE INTEGRATION
==================================================

Design and implement the safest integration point supported by the repository.

Possible trigger points may include:

- PR event
- merge event
- staging deployment
- release/deployment completion

Choose based on existing architecture and John's requirement.

IMPORTANT:

Do not introduce automatic production deployment.

Do not require real GitHub secrets for local implementation/testing.

If actual GitHub integration requires credentials/secrets:

Implement the code/configuration contract safely and mark live execution:

DEFERRED — REQUIRES INTEGRATION ENVIRONMENT

Never print or hardcode secrets.

==================================================
STEP E9 — FAILURE SAFETY
==================================================

Knowledge generation must NOT break application deployment.

Design failure behavior explicitly.

Examples:

If KB generation fails:

- record/report failure
- preserve diagnostic information
- avoid creating corrupt/partial published content

Determine whether it should block deployment only if existing approved requirements say so.

Do not invent a deployment-blocking policy.

==================================================
STEP E10 — LOCAL TESTING
==================================================

Create local fixtures/tests representing:

Scenario 1:
PR with valid linked PRD/docs
→ structured KB draft generated.

Scenario 2:
PR with insufficient documentation
→ safe failure / actionable result.

Scenario 3:
Same PR processed twice
→ no uncontrolled duplicate.

Scenario 4:
Existing draft from same source
→ safe update/no-op according to architecture.

Scenario 5:
Existing published article
→ not silently overwritten.

Scenario 6:
Generated article
→ remains draft/unpublished.

Scenario 7:
Unauthorized caller
→ cannot abuse generation endpoint/process.

Scenario 8:
Traceability metadata
→ correctly retained.

==================================================
STEP E11 — PRESERVE FUTURE MODEL-AGNOSTIC DESIGN
==================================================

John wants the architecture to remain model-agnostic.

Do not tightly couple the pipeline to one AI provider unless the repository explicitly requires it.

Separate:

SOURCE COLLECTION

from

CONTENT TRANSFORMATION

from

KB PERSISTENCE

from

APPROVAL/PUBLISHING.

If AI generation is used or planned, keep the provider boundary replaceable.

Do not implement unnecessary “AI-in-a-box” architecture.

==================================================
STEP E12 — REGRESSION
==================================================

Verify no regressions to:

- TOW app
- KB navigation
- authentication
- Phase C permissions
- Phase D lifecycle
- hierarchy
- article reading
- article creation/editing
- approval/publishing
- existing CI/CD

Run locally available:

- typecheck
- lint
- unit tests
- integration tests
- build

Fix only regressions caused by Phase E.

==================================================
STRICT SCOPE
==================================================

DO NOT implement Phase F yet.

Specifically do NOT implement:

- LangSmith agent migration
- IT/HR agent retrieval changes
- bulk Word/PDF ingestion
- full MCP retrieval redesign
- TOW notification work
- TOW shared-list work
- unrelated features

DO NOT:

- commit
- push
- merge
- create PR
- deploy
- apply staging/prod migrations

==================================================
FINAL REPORT
==================================================

Return:

PHASE E STATUS:
COMPLETE / PARTIALLY COMPLETE / BLOCKED

Report:

1. Existing GitHub/CI-CD architecture discovered
2. Documentation sources discovered
3. Generation architecture implemented
4. Article draft contract
5. Trigger/integration design
6. Duplicate/idempotency handling
7. Traceability implementation
8. Phase C permission integration
9. Phase D lifecycle integration
10. Security behavior
11. Tests and exact results
12. CI/CD regression status
13. Items requiring real GitHub/staging integration later

Clearly separate:

LOCAL IMPLEMENTATION COMPLETE

from:

DEFERRED INTEGRATION VALIDATION

List any:

QUESTIONS FOR JOHN

Only if a genuine business decision is unresolved.

Finally state:

READY FOR PHASE F LOCAL IMPLEMENTATION: YES/NO

STOP.

Do not start Phase F.
Do not commit.
Do not push.
Do not merge.
Do not deploy.
