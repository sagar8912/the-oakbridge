
Proceed with Phase 3.

Do not change the existing architecture.

Goal:
Implement the missing MCP tool kb_list_categories.

Requirements:

1. Create a new MCP tool named:
   kb_list_categories

2. Do not create any new backend API.

3. Reuse the existing endpoint:
   GET /api/retrieval/hierarchy

4. Transform the hierarchy response into a flat category list.

Example output:

{
  "success": true,
  "tool": "kb_list_categories",
  "categories": [
    {
      "id": "...",
      "name": "...",
      "kb_code": "..."
    }
  ]
}

5. Add:
   - tool description
   - examples
   - Zod input/output schema
   - MCP annotations

6. Register the tool in the existing registry.

7. Keep backward compatibility.

8. Do not modify existing APIs.

9. Add unit tests.

After implementation provide:
- Files changed
- Registration changes
- Example MCP response
