
Act as a Principal AI Engineer and Full-Stack Architect.

We are migrating the KB Admin chatbot from OpenAI to Anthropic Claude.

The chatbot is currently implemented using OPENAI_API_KEY and the OpenAI SDK.

I want you to refactor the implementation to use ANTHROPIC_API_KEY and the official Anthropic SDK while preserving all existing functionality.

Requirements:

1. Remove all OpenAI-specific code.
   - Remove OpenAI SDK imports.
   - Remove OpenAI client initialization.
   - Remove all ChatCompletion / Responses API calls.
   - Remove OpenAI-specific request/response parsing.

2. Replace with Anthropic.
   - Use the official @anthropic-ai/sdk package.
   - Initialize the client using:
     process.env.ANTHROPIC_API_KEY
   - Use Claude Messages API.

3. Keep the chatbot UI exactly the same.
   - Do not change React components.
   - Do not change styling.
   - Do not change routing.
   - Do not change chat history behavior.

4. Preserve all chatbot capabilities.
   - Streaming responses (if already implemented).
   - Conversation history.
   - Context injection.
   - KB retrieval.
   - RAG pipeline.
   - Citations.
   - Session handling.
   - Error handling.
   - Typing indicator.
   - Retry logic.

5. Replace environment variables.
   Remove:
   OPENAI_API_KEY

   Add:
   ANTHROPIC_API_KEY

6. Update backend API routes to call Claude instead of OpenAI.

7. If embeddings are currently generated using OpenAI:
   - Do NOT silently replace them.
   - Clearly identify every embedding dependency.
   - Keep embeddings unchanged unless required.
   - Explain which embedding provider is currently used.

8. Update configuration files.
   - package.json
   - env validation
   - docker
   - deployment configs
   - README
   - setup documentation

9. Update all helper utilities that reference OpenAI.

10. Maintain the existing system prompt, RAG prompt, and prompt engineering exactly as before.

11. Preserve hallucination protection.
   The chatbot must:
   - Answer only from KB Admin/TOW knowledge.
   - Never fabricate information.
   - If information is unavailable, respond:
     "I couldn't find this information in the Knowledge Base."
   - Never invent APIs, pages, workflows, or permissions.

12. Preserve source attribution and citations if implemented.

13. Keep authentication, authorization, and permission checks unchanged.

14. Use the latest supported Claude model through Anthropic.

15. Do not introduce any breaking changes.

Deliverables:
- List every file modified.
- Show every OpenAI dependency removed.
- Show every Anthropic dependency added.
- Show the updated environment variables.
- Show the exact npm/pnpm package to install.
- Confirm that all chatbot features continue to work after migration.
- Ensure the project builds successfully with no TypeScript or lint errors.
