Act as a Principal Full-Stack Engineer.

Implement Phase 2 (Integration) for the KB Admin Permission Management.

Goal:
Connect the Permission Management UI to the real backend permission APIs and database. Remove any mock, placeholder, or local-only state.

Requirements:

1. Load data
- When the Permission drawer opens, fetch the selected user's permission profile from the backend.
- Load:
  - KB Role
  - Feature permissions
  - Workflow permissions
  - Administration permissions
  - Knowledge Base scope
  - Team assignments

2. Save
- Clicking Save must call the real backend API.
- Persist all permission changes to the database.
- Show loading state while saving.
- Show success/error toast.

3. Refresh
- After saving, close/reopen the drawer or refresh the page.
- Verify the saved values are loaded from the database.

4. Validation
- Handle API failures gracefully.
- Prevent duplicate save requests.
- Disable Save while saving.
- Only send changed values if supported.

5. Remove mock logic
- Remove hardcoded permissions.
- Remove local-only state used for demo purposes.
- Ensure all displayed values come from backend APIs.

6. Authorization
- Respect backend authorization rules.
- Prevent unauthorized users from changing permissions.
- Display appropriate error messages.

7. Code quality
- Follow existing project architecture.
- Reuse existing API clients and services.
- Keep TypeScript types consistent.
- Do not introduce breaking changes.

Deliverables:
- List every modified file.
- Specify which API endpoints are used.
- Specify which database tables/RPCs are updated.
- Explain the end-to-end request flow.
- Do NOT commit, merge, push, or deploy anything.
